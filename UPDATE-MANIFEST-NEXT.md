# Wave-next integration manifest

Branch: `update/wave-next`
Base: `origin/master` (`b411d70004`)

## Pending items

- Chat-relay rework
- PR #1158 rebase and teambar telemetry fold-in
- WF menu P1+P2
- Units-per-group follow-ups
- Recycle residue

## Folded items

- PR #1158: rebased over current master, preserved the merged skin-swap fix without duplication, and merged no-ff into wave-next as `81e42f2a5414c6574fb8b0c7331869ba0034a7da`.
- PR #1195: reviewed as report-tooling-only after isolating its stacked wave history; rebased head `e09027b48a` was merged directly to master as `e1be2c69818b006fa20e2f3a62052905ec565725`, not folded into wave-next.
- PR #1202: autorun built + Sol-reviewed 2 rounds (3 blockers + 1 follow-up fixed); lint gained `stance` in A3CMD; merged no-ff into wave-next as `f5d3b97d1b96f8bb00de5dbb786578aff586909f` (feature head `67b1892793`).
- PR #1207: council quick-wins: 8 groups, including the confirmed dead-camp gate residual fix; merged no-ff as `4562e2879fe17dc5512672c5f69654ed31a9abe1` (feature head `2b1e105848`).
- PR #1206: WF menu P1+P2: hub polish + consistency + IDD collisions 24100/10202, in-engine visual smoke owner-gated; merged no-ff as `22ff5c5a09e8b07ac1b65e026435616efd959e82` (feature head `d003c453a7`).
- PR #1204: server-events one-way chat relay, flag-dark (`WFBE_C_CHAT_RELAY=0`), reviewed in 3 rounds; merged no-ff into wave-next as `b845fbb86ed4c0a09efed5118dea46aef360ba52` (approved head `9f323f77bc`).
- PR #1210: always-offense review blockers fixed (flag-0 relief, staging watchdog, truthful funds telemetry, and FOOT_STAGE FLAGGATE guard); merged no-ff into wave-next as `a6a7662ff85f4ee54778637af5761dad05c72de5` (approved head `1432dd851f`).
- PR #1205: roster phase-2: 3 flag-dark features (E3 GUER ground QRF, E5 late Huey QRF, and per-town airfield/high-SV garrison overlays), Sol-approved after 2 review rounds; merged no-ff into wave-next (approved head `ec68ecf014`).

- PR #1162: approved CIV HC slots-only mission.sqm change; merged no-ff as `de7475e9d9` (rebased feature head `5d9ca7c052`).
- PR #1208: approved C3 consensus quick-wins (HC census/no-HC accounting and flag-dark telemetry); merged no-ff as `8cddb617cc` (feature head `dc6b9afd59`).
- PR #1161: approved Zargabad small-map tune; merged no-ff as `7d857f06d9` (final reviewed head `d4ffe51adf`).
- PR #1160: approved AICOM air founding telemetry; merged no-ff as `8a81ab3392` (final reviewed head `b7733e82e5`).
- PR #1155: approved B76 funds-resend zero-wallet fix (never invent a zero wallet on CIV-drift, authority-safe resolution, no side-adoption); merged no-ff into wave-next as `c53cef8019` (drain head `ff631ee434`).
- PR #1157: VEHDEL probe proper - duplicate gc-zombie emission dropped, FLAGGATE-conform probe guard, truthful default-0 header; replaces the wave0720c shim path; merged no-ff into wave-next as `dfb9cd98e0` (drain head `fefacf451b`).
- CORRECTION (fix/vehdel-fold-corrective, 2026-07-21, fold-preservation audit): the merge above (`dfb9cd98e0`) actually folded iteration `71ef2feec1`, not the claimed drain head `fefacf451b` - missing the duplicate gc-zombie-unit VEHDEL emission drop and the truthful default-0 header comment. This PR restores both reviewed deltas.
- PR #1159: mobile-artillery echelon reposition decoupled from the single-fire gate - gun #2 no longer starved of repositioning; single-fire-per-cycle preserved; merged no-ff into wave-next as `a9d166c137` (drain head `81db315180`).
- PR #1156: aicom-town-nudge _x rebinding fix w/ nil-guard-first capture; merged no-ff into wave-next as `526eacce65` (drain head `57461d1510`).
- PR #1211: GUER client startup (wfbe_commander marker on GUER logic - client loop/AFK/chat now start) + Barrel-Bomb/SCUD designation cancel with default map-click handler restore; merged no-ff into wave-next as `377cb2b667` (feature head `fc53d364f7`).
- PR #1151: client name-tag overlay candidate caching + TAGSTAT telemetry; test collection fixed; merged no-ff into wave-next as `7bbd762d4d` (feature head `98442f67bf`).
- PR #1212: STOPPED - not folded. Approved head `28cb4e0ae9` conflicts non-trivially with the #1159 echelon fold already on this branch: both rewrite the same base-artillery counting block in `AI_Commander_Base.sqf` (CH/TK/ZG) with incompatible logic (echelon-registry branch vs global vehicles-scan replacement). Needs owner/reviewer reconciliation, not a textual merge. NOTE: the `fix/aicom-arty-cap` branch tip has also moved to `7a12a0e0a5` (husk-reaping/map-label follow-up) which is NOT covered by this approval - re-verify head before any retry. SUPERSEDED (see PR #1221 below): cap commit reconciled by #1221; husk/label commits re-landing separately via #1220. PR #1212 closed.
- wallet: RequestAIComDonate derives donor team server-side (arbitrary-group drain closed) + authority regression test; PV-sender residual owner-accepted, token infra carded next wave; merged no-ff into wave-next as `af840699b0` (drain head `794de3b476`).
- PR #1216: wrecks no longer hold/contest towns (alive+crewed filter in the capture tally) + town statics locked and salvage-proof (keepAlive at creation) - owner rulings; merged no-ff into wave-next as `c761d0e5c7` (feature head `9aab2e5097`).
- PR #1218: quartermaster passive top-up free under a human commander (WFBE_C_AICOM_TOPUP_HUMAN_COST default 0); AI-commanded sides unchanged - owner ruling; merged no-ff into wave-next as `64e20db2b9` (feature head `314ceb4e39`).
- PR #1154: commander-lease stand-down relocated OFF the JIP flow (owner constraint) into the connect handler, reachability-fixed above the duplicate-connect latch, order-asserted by test; dedicated-server-scoped (documented); merged no-ff into wave-next as `a1474fe84c` (drain head `c0a1109ec6`).
- PR #1219: XS bundle - rare-GUER squad survives merging, US patrol crew entry, Init_Markers HANGGUARD + airfield radio dub fixes (all terrains), founding size 10 (owner ruling), USMC/RU pooled garrison variants; merged no-ff into wave-next as `b0fc2ff71a` (feature head `d24429067b`).
- PR #1221: AI commander artillery cap enforced in BOTH echelon modes (registry when ON, global ownership-tagged scan when OFF - MHQ relocation no longer defeats the cap); supersedes #1212's cap commit; merged no-ff into wave-next as `f624692ad7` (feature head `11ea698d16`). NOTE: this intentionally invalidates `Tools/Lint/test_aicom_artillery_echelon.py::test_07_flag_off_source_paths_are_byte_identical_to_legacy`, which pins the OLD buggy legacy near-HQ scan verbatim in the flag-off path - that assertion is now stale and should be updated/removed, not a regression (14/15 tests in that file still pass; all echelon-ON behavior unchanged).
- PR #1215: paratroopers get post-drop patrol/combat orders (AI drops only) + fixed-wing air responses spawn at FLY (no more near-stall A10/Su34/AV8B/Su25); merged no-ff into wave-next as `da262ba422` (feature head `8788476075`).
- PR #1214: pack-time guard against init.sqf contamination (canonical missions ship NO init.sqf - engine auto-runs initJIPCompatible.sqf; guard aborts the pack on any root init.sqf, case-insensitive); merged no-ff into wave-next as `f4dbe3c375` (feature head `d044456d97`).
- PR #1217: dead-camp presence repair (owner redesign) - any WEST/EAST/GUER soldiers standing in a destroyed camp's bubble ~2.5min restore it (flag WFBE_C_CAMP_REPAIR_PRESENCE, ships default-OFF; civilians excluded, radius parity with capture rules, stale-clock + null guards); merged no-ff into wave-next as `274be6f4e7` (feature head `663368b711`).
- PR #1213: fast-travel billing fixed - vehicle surcharge no longer always-$0, crews billed once per vehicle not per seat, players charged exactly the quoted convoy (revalidated at confirm; late arrivals need a fresh quote) + camp flag now counts within the capture radius (5.5 to 8m); merged no-ff into wave-next as `8ab3eb88f2` (feature head `a7497fab87`).
- PR #1222: echelon test_07 re-pinned to the reconciled off-path global scan (+ legacy-pattern regression guard); merged no-ff into wave-next as `de19fb1028` (feature head `2d28b44cd7`). Resolves the test-staleness note from the #1221 fold - Tools/Lint/test_aicom_artillery_echelon.py now 15/15.
- PR #1209 (manifest completeness fix - merge predates this manifest's tracking): Takistan arrivals recovery pack - TK-specific foot/mounted AI speed recalibration (0.9/3.5 m/s vs the 2.2/7.5 baseline), TK route-factor bump (1.5 -> 2.5), and TK assault-timeout ceiling retuning (1500 -> 2700s) for the ridge terrain's worse detours; adds `WFBE_C_AICOM_STRAND_RECOVERY` (dark, default 0 - one-shot recovery redispatch after a short-move stranded verdict) and a foot-hold dark flag (shipped as `WFBE_C_AICOM_FOOT_HOLD`, later reworked/renamed to `WFBE_C_AICOM_FOOT_STAGE` offensive staging by #1210); merged as `6eaa84b762` (+ hardening `42c51e20a7`: cap-lock/recycle guards so strand-recovery never re-dispatches a locked or recycling team, a blacklist-preserving fallback when recovery has no alternate candidate instead of clearing it, and a `_side < 0` guard alongside the existing `isNil` check in `Server_AI_SetTownAttackPath.sqf`).
- PR #1223: corpses no longer hold/contest towns (alive filter on the capture tally's Man branch - completes the #1216 dead-things-don't-vote ruling) + WFBE_C_AIR_ATTACK_GUNNER comment corrected to reflect armed status; merged no-ff into wave-next as `2121b26e3f` (feature head `85edcdf96e`). Known nit: the block comment at server_town.sqf:112 still says Man entries pass through untouched - cosmetic, next hygiene pass.
- PR #1203 (manifest completeness fix - merge predates this manifest's tracking): guard nil team/side in Server_AI_SetTownAttackPath.sqf dispatch inputs - a culled-dispatch guard (`isNull _team || {isNull _town_assigned}`) exits before the leader/getPos dereference, and an `isNil "_side"` exitWith guard stops dispatch with an undefined side (the fix for the live '_side nil at SetTownAttackPath' RPT family); each path logs one always-on WARNING via WFBE_CO_FNC_LogContent; CH/TK/ZG confirmed byte-identical. Later hardened by #1209 (widened to `isNil "_side" || {_side < 0}` plus a fallback-leader `side` recovery attempt before giving up - see the #1209 entry above). Merged as `72d6873f41`. NOTE: this intentionally invalidates `Tools/Lint/test_settownattackpath_nilside.py`, which pins the ORIGINAL narrower `if (isNil "_side") exitWith {` text verbatim and now crashes with an uncaught `ValueError` (not a graceful assert failure) when #1209's widened guard text doesn't match. Manually re-verified all of that test's underlying invariants (ordering, WARNING/LogContent counts, CH/TK/ZG byte-identity) against the current source with the widened pattern substituted - all still hold; this is test staleness from the #1209 hardening, not a functional regression. Whoever owns this test file should update the pinned string, same class of issue as the #1221/#1222 echelon test_07 fix.
- PR #1224: SCUD fire-mission map-click gets the full cancel/restore treatment (latch + map-close watcher + default-handler restore on every path) - closing the misclick-fires-a-strike hazard on all maps (the flow is live everywhere via WFBE_C_SCUD_DRIVABLE_ALLMAPS=1); merged no-ff into wave-next as `91fcb5b85f` (feature head `6f03d9f5ff`).
- PR #1220: commander artillery husks now reaped - dedicated age-gated reaper (dynamic delay: lobby cleanup timeout +180s, 300s floor) with locality-correct handling (server-local direct delete; HC-local via the owner-routed dispatch channel w/ tag-checked receiver; PV-sender forgery residual owner-accepted + documented, token infra carded) + commander artillery teams now labeled HQ TEAM [ARTY] on the map (both marker passes); merged no-ff into wave-next as `483d4d9247` (feature head `793aff27e8`). Supersedes the husk/label commits originally proposed in the since-closed #1212.
- PR #1221 follow-up: steady-state artillery build-skip AICOMLog lines ('at cap', 'no SELF-PROPELLED class') debounced with the per-reason-per-side 300s idiom + cumulative count in the emitted line; logging-only; merged no-ff into wave-next as `4efa4ceca1` (feature head `aaa539bd8f`, one commit past the already-folded #1221 head `11ea698d16`).
- PR #1225: repaired the crashed nil-side ordering test (now pins the full #1209 guard shape) + FOUND_SKIP telemetry emission now respects WFBE_C_AICOM_C3_TELEMETRY (armed at release; flag-correctness not silencing); merged no-ff into wave-next as `d6e5782b8b` (feature head `2bad417ddd`). Resolves the crashed-test note from the #1203 manifest entry - Tools/Lint/test_settownattackpath_nilside.py now PASSes.
- PR #1227 (LAST CONTENT FOLD OF WAVE0721): wave0721 ARMING COMMIT - all 19 wave-added feature flags flipped 0->1 per the owner's "everything flags on, no dark new stuff" ruling (GUER QRFs, town overlays, AICOM telemetry/tuning/strand-recovery/foot-stage/arty-echelon, camp presence-repair, town-flip broadcast, probes, commander lease, chat relay, COMMAND V2 nudge/doctrine/posture/support-air); 2 deliberate exclusions stay 0: AICOM_TOPUP_HUMAN_COST (0 IS the owner-ruled free-refit value) and CMD_SUPPORT_JET (reserved, no code path); completeness independently re-verified against all 51 wave-added constants at review time; merged no-ff into wave-next as `802462a9cb` (feature head `13b43db42f`). VERIFICATION: hand-counted all 19 `0->1` diff hunks against the description (exact match) and confirmed both exclusions are absent from the diff and still read 0 with their documented rationale intact in the merged source. EXPECTED TEST FALLOUT (not regressions - this is the direct, intended consequence of arming): test_cmd_v2_nudge.py (12 failures - 4 COMMAND V2 master flags x 3 terrains), test_aicom_artillery_echelon.py (1), test_commander_lease.py (1), test_veh_delete_probe.py (1 of its 2 - the kill-switch-defaults-off check), test_chatrelay_rework.py (1) all pin a literal `= 0` default that this commit correctly flips to 1; harmless, whoever owns test hygiene should re-pin post-arming. SEPARATE FINDING (genuine, pre-existing, unrelated to this fold): test_veh_delete_probe.py's OTHER failure (test_ratchet_manifest_matches_the_entire_tree) shows Tools/Lint/vehdel_inventory.json is stale against server_groupsGC.sqf's deleteVehicle call sites - traced to the already-folded #1220 reaper adding new delete paths (server-local + HC-dispatch) after the manifest was last generated during the #1157 VEHDEL fold. Needs `vehdel_inventory.json` regenerated by whoever owns that tooling; not a code defect.
- PR #1228: post-arming test re-pins (armed defaults =1 incl. teambar; armed/dark master-flag split preserves CMD_SUPPORT_JET dark) + vehdel ratchet manifest regenerated for the #1220 reaper sites; test/tooling only; merged no-ff into wave-next as `b3c90c5b69` (feature head `41f1cbad54`). Resolves both classes of test fallout flagged in the #1227 entry above - all 6 touched test files re-run clean (48/48, 15/15, 18/18, 5/5, 7/7, and test_chatrelay_rework.py exits 0).
- PR #1229: restores the reviewed #1157 content a stale fold iteration dropped - duplicate gc-zombie VEHDEL emission removed (one emission per zombie delete, not two), truthful default-0 header restored in Common_LogVehDelete.sqf; manifest correction note added above (see the #1157 entry) documenting that the merge actually folded iteration `71ef2feec1`, not the claimed drain head `fefacf451b`; merged no-ff into wave-next as `76767e6804` (feature head `fc76db3141`). Verified the diff matches the description exactly (single duplicate-call removal + comment fix). Lint: 6 touched SQF files, 0 findings. FOLD-ORDERING ARTIFACT (found, not a bug in either PR): folding #1228 then #1229 in this sequence leaves Tools/Lint/vehdel_inventory.json stale again by exactly one field - #1228's manifest regen captured server_groupsGC.sqf's `probed` count as 6 (the pre-#1229 state, still with the duplicate call), and #1229's removal of that duplicate drops the true count to 5. Precisely isolated: `total` (13) matches, only `probed` (6 vs actual 5) differs, single file (Server/FSM/server_groupsGC.sqf) only. Needs one more regeneration pass; not touched here since it's the same test/manifest-tooling class of finding as the prior two.
