# WASP Improvement Plan (all facets) — LIVE BUILD: B66

_Seed v1: 2026-06-20 overnight multi-facet adversarial debate (15 agents, deeply code-verified). LIVING doc: refined every ~2h overnight. **UPDATED TO B66 2026-06-21 (cycle 6)** — live server is now B66 (own-side MARKER fix series; gameplay unchanged from B59-B61). PROPOSE-ONLY — nothing here is deployed by me; builds are deployed by Ray._

**Changelog**
- _2026-06-21 (cycle 6 — RE-ANCHOR TO B66 + aicom-units bleed/asymmetry deep-dive): live is now **B66** (markers fixed; gameplay = B59-B61). Fresh read-only RPT pull: **srvFPS 31-39** mid-game (407-433 units, 7 active towns), 45-47 at warmup, HC-2 fps 25; **0 script errors, 0 ASSAULT_STRANDED, 0 STUCKSTAT, 0 MHQRELOC** this window. WEST is **PRESSING** and took its first town (Lopatino, bootstrap stipend ended cleanly); EAST is on **DEFEND** and bleeding — it issued a **BATCH of ~11 `retreat-and-reform` orders at one tick, all single-survivor (alive=1)** teams. ORBAT: WEST armor 4 / personnel 157 vs EAST armor 1 / personnel 120. CMDRSTAT WEST 15 teams @ 6.4, EAST 14 @ 5.7. One self-corrected `SPEARHEAD_REPICK|WEST` (Myshkino→Lopatino, captured next tick — working as intended). **TWO deep debates this cycle (most-implicated + B66 reconciliation):** **(2/3) the alive=1 churn + EAST bleed**, and **(7) reconcile the stale-B65 plan against what B66 actually shipped.** **KEY NEW DISCOVERY — a telemetry contradiction:** `retreat-and-reform` fires from EXACTLY one place (`AI_Commander_Produce.sqf:89-95`) and is gated OFF for HC teams (`:78` needs `_canProduce` = `!isPlayer ldr && !wfbe_aicom_hc`). Yet CMDRSTAT reports **0 srvTeams** while ~11 retreat lines appear — those two facts can't both be true. Either the srvTeams counter is wrong, or the `wfbe_aicom_hc` tag isn't propagating on re-adopted teams. **This is the cheapest, highest-value unlock in the plan: a read-only `tag=` on the retreat log (`Produce.sqf:94`) + a per-side RETREATSTAT counter settles it for one `diag_log` string.** Root cause behind the bleed: **HC-founded teams have NO refill path at all** — B57 pads them once at founding (`AI_Commander_Teams.sqf:363-391`); after that attrition is one-way (the B61 refill-at-base loop, `Produce.sqf:96-237`, is server-local-only). **B66 reconciliation results:** air-gate 4→3 CONFIRMED+CLOSED (`Init_CommonConstants.sqf:166`); heli cannon-nudge concern now **MOOT** (B66 scoped it to heli-teams-only + fixed the muzzle, `Common_RunCommanderTeam.sqf:123-173` — do NOT re-propose disabling); B66 added MHQ **player/orphan** guards (`AI_Commander_MHQReloc.sqf:117-134, 191-193`) but they're player-facing — **plan #1 (MHQ deploy-time ENEMY re-check) is STILL GENUINELY OPEN**, exact insertion just before the deploy ExecVM at `:238` (`_enemyClear`/`_enemySide` already in Spawn scope). **Q2 verdict: DO-NOT-TOUCH-BALANCE STILL HOLDS** — income is symmetric + town-supply-keyed (`updateresources.sqf:56`, no per-side constant); EAST's bleed is emergent (map position) + a **piecemeal-commit tactics** symptom of the no-HC-refill root, not an economy asymmetry. Reject income tunes AND reject "wait-to-mass" staging (frozen-AI hazard)._
- _2026-06-21 (cycle 5 — MHQ-RELOC FIRED LIVE + FPS-at-15-teams deep-dive): live was **B65**. **THE LANDMINE FIRED:** `MHQRELOC|WEST|63|TRIGGER|frontDist=3531|...back=800` → `MOBILIZED` — first fire, no stranding, no errors → **CONDITIONAL-GO**, the deploy step ships with no enemy re-check. **FPS at 15 teams:** `SRVPERF|55|fps=15|units=530|activeTowns=6` — a spawn-burst, not steady-state → **ADOPT staggering town activations**. unitsPerTeam 6.9-7, 0 stranding._
- _2026-06-21 (cycle 4 — RECONCILE TO B61 + economy & spectacle): live was **B61**. Front MOVING not frozen → B59 freeze fixes demoted to insurance. **Economy verdict: CHANGE NOTHING + measure** (FUNDS-FALLBACK is the intended funds→tech converter). **Spectacle verdict: consume the RPT firehose OFF-SERVER** (Front-Status embed → LLM recap → timelapse). NEW TOP RISK flagged: B61 ships MHQ-reloc default-ON, hadn't fired yet._
- _2026-06-21 (cycle 3): sharpened strand root cause to a terrain-blind SPAWN wedge (`distStart=0`). Superseded by B61+ evidence._
- _2026-06-21 (06:00 CONSOLIDATION, B59): measured retreat-and-reform death loop at 6,397 orders, 100% alive=1. B59-specific; refill-at-base mitigated the server-local path._
- _2026-06-21 (cycle 2): economy symmetric BY CONSTRUCTION; B60 MHQ build diverged → HOLD/re-build._
- _2026-06-21 (cycle 1): surfaced alive=1 retreat-and-reform churn; added #1 (disband alive≤1), strand teleport._

---

## Headline

**State of B66 — healthy, asymmetric, and the bleed has a NAME now.** Live B66 is mechanically strong: **srvFPS 31-39 mid-game (47 quiet), 0 script errors, 0 ASSAULT_STRANDED, 0 STUCKSTAT**, HCs absorbing the AI load, full **15/14 founded teams** both sides. The front is **MOVING, not frozen** — WEST is pressing, took Lopatino, EAST is on the back foot. The markers series (B63-B66) is confirmed working by Ray and is **not** this debate's focus.

**The fresh signal: EAST is bleeding, and we finally have the mechanism.** EAST issued a **batch of ~11 `retreat-and-reform` orders at a single tick, every one a single-survivor (alive=1) team**, while holding 1 armor / 120 personnel against WEST's 4 / 157. Code-deep review this cycle finds the bleed's root: **HC-founded teams have NO refill path.** B57 pads infantry to found-size **once**, at founding (`AI_Commander_Teams.sqf:363-391`); the B61 refill-at-base loop that mitigated the old death-loop is **server-local-only** (`AI_Commander_Produce.sqf:96-237`) — it never touches the HC teams that are ~100% of the field. So HC teams attrit one-way, trickle in as alive=1, retreat, and re-found from scratch instead of topping up. That's slow, FPS-cheap churn (founding is throttled), not a death spiral — but it's why EAST commits piecemeal and gets ground down.

**A telemetry contradiction is gating the fix.** `retreat-and-reform` is emitted from exactly one place (`Produce.sqf:89-95`) and is gated OFF for HC teams (`:78`). Yet CMDRSTAT reports **0 srvTeams** while ~11 retreat lines fire. Both can't be true. Either the srvTeams counter mislabels, or `wfbe_aicom_hc` isn't propagating on re-adopted teams. **The unlock is one read-only line:** tag the retreat log with the team's `wfbe_aicom_hc`/`founded` flag + a per-side RETREATSTAT counter (`Produce.sqf:94`). It converts a guess into a measurement for the cost of a `diag_log` string-build, and it decides whether the real fix is "wire HC refill" or "fix a tag-propagation bug." **This is now the #1 action.**

**B66 closed three of last cycle's open questions; one survives.** Air-gate 4→3 is confirmed and closed; the heli cannon-nudge worry is **moot** (B66 scoped it to heli-teams-only and fixed the muzzle — do not re-propose disabling it); B66's new MHQ guards are real but **player/orphan-facing**, so **plan #1 — the MHQ deploy-time ENEMY re-check — is still genuinely open**, now with an exact one-block insertion point (`AI_Commander_MHQReloc.sqf:238`, with `_enemyClear`/`_enemySide` already in scope). That stays the highest-leverage code change.

**This cycle's two debates (most-implicated by the fresh RPT):**
- **(2/3) aicom-units bleed + per-side asymmetry:** the alive=1 churn is HC teams with no refill; the EAST bleed is emergent map position + piecemeal-commit, **not** an economy asymmetry. **ADOPT two read-only diagnostics now** (retreat-tag/RETREATSTAT + infPerTeam/vehPerTeam split + per-side town-supply); **TRIAL wiring the already-written `AI_Commander_HCTopUp.DRAFT.sqf`** only AFTER the diagnostic confirms HC teams are the churners; **REJECT the flag-then-HC-delete reclaim** (#8 — it deletes live combat units); **DO-NOT-TOUCH-BALANCE holds.**
- **(7) B66 reconciliation:** re-anchored the plan; closed air-gate + heli-nudge; kept MHQ deploy-time enemy guard open with a concrete insertion line.

---

## Top actions (all facets, ranked) — cycle-6

| # | What | Why (B66 evidence) | Impact / Effort | Verdict | Facet | A2-OA-safe pointer |
|---|------|--------------------|-----------------|---------|-------|--------------------|
| 1 | **Tag the retreat-and-reform log + per-side RETREATSTAT counter (read-only)** — append the team's `wfbe_aicom_hc`/`wfbe_aicom_founded` flag (and a team ref) to the `retreat-and-reform ordered` line at `AI_Commander_Produce.sqf:94`, and emit one `RETREATSTAT|<side>|count=..` per AICOMSTAT tick | **The telemetry contradiction:** retreat fires only for server-local teams (`:78`), yet CMDRSTAT shows **0 srvTeams** AND ~11 EAST retreat lines. This one line settles whether the churners are HC teams (→ wire refill, #6) or mis-tagged server-local teams (→ a propagation bug). **Cheapest, highest-value unlock in the plan** | **highest** / very-low | **adopt** | aicom-units / bugs-telemetry | `diag_log`+`str`+`+` only; no banned cmds; paced by the existing 1-team/cycle founding cadence — never per-frame |
| 2 | **Add a deploy-time ENEMY re-check to MHQ-reloc** — re-run the trigger-time enemy-clear idiom (`AI_Commander_MHQReloc.sqf:84-86`) against `_finPos` immediately before the deploy ExecVM at **`:238`** (B66 line); if the tile is hot, deploy-in-place / redeploy backward. Keep default-ON | **STILL OPEN after B66.** The landmine FIRED in B65 and survived only because the dest was clear; B66 added player/orphan guards (`:117-134, 191-193`) but NO enemy re-check. The deploy step still re-checks nothing after a ≤600 s drive; the respawn anchor is coupled (`Client_GetRespawnAvailable.sqf:8-9`). `_enemyClear`/`_enemySide` are already in Spawn scope — zero new plumbing | **highest** / low | **adopt** | aicom-strategy / B60 | re-use existing `:84-86` block; always converge on a deploy (never a frozen MHQ); no banned cmds. Optional TRIAL: STANDOFF 800→1000-1200 |
| 3 | **infPerTeam/vehPerTeam split + per-side town-supply line in CMDRSTAT (read-only)** — split the blended `unitsPerTeam` into inf/veh, and log each side's total `GetTownsSupply` that feeds income | CMDRSTAT's single mean can't tell "14 healthy 6-man teams" from "8 full + 6 alive=1 stragglers" — same mean, very different war. The split + town-supply line PROVES whether EAST's bleed is distributional vs aggregate, and whether the economy gap is real before anyone touches balance | high / low | **adopt** | bugs-telemetry / economy | `diag_log` string-build only; emit on the existing CMDRSTAT/income tick, never per-unit |
| 4 | **Stagger town activations to flatten the FPS spawn burst** — per-sweep budget so ≤2 *new* towns activate per 5 s pass in `server_town_ai.sqf` (~`:142-187`); rest wake next sweep | B65 high-load: `fps=15 units=530 activeTowns=6` — a 5-6-town simultaneous spawn/scan burst, not steady-state. B66 mid-game holds 31-39 at 7 towns/433 units, so the burst is the only sag. **No town denied, AI still wakes on proximity** | high / low-med | **adopt (trial on test box)** | performance | plain counter + `time` debounce (idiom `:155`); NOT lowering `TOWNS_ACTIVE_MAX`. Verify ≤10 s defender-spawn lag on a 3rd simultaneous town |
| 5 | **SUPPLYSTAT diagnostic line** — periodic per-side `SUPPLYSTAT|v1|<side>|<min>|reserve=..|income=..|fallbackCount=..|towns=..`, gated to the income tick (~60s) | No periodic per-side aggregate exists to PROVE tempo is healthy. One cheap line turns the next economy debate from anecdote into data; pairs with #3's town-supply line | high / low | **adopt (trial)** | economy / bugs-telemetry | `diag_log`+`str`+`+`; counter via `setVariable` at `Server_AI_Com_Upgrade.sqf:134`; income interval, never per-frame |
| 6 | **TRIAL: wire the existing `AI_Commander_HCTopUp.DRAFT.sqf` (refill HC teams in place)** — write the unbuilt HC-side consumer (`:144-165`), keep default-OFF, 1-team/call, in-contact + near-supply guarded. ADD units via createUnit+joinSilent next to the leader (no delete, no re-found) | **The ROOT-CAUSE fix for the bleed:** HC teams have no refill, so they trickle in as alive=1. Refilling in place stops the retreat churn AND the piecemeal-commit at source — serves both Q1 and Q2. **GATED behind #1** confirming HC teams are the churners | high / med | **trial (gated on #1)** | aicom-units | add-only (no deleteVehicle, no leader churn); `behaviour=="COMBAT"` + near-supply guards exist (`:82,:86`); A2-OA-safe by construction (`:26-34`). Watch for a funds-paid-but-bus-dropped soft leak (`:129`) |
| 7 | **TRIAL: shrink `restorer_buildings` scan to per-HQ bubbles** — replace the 10.5 km `nearestObjects` (`buildings_restorer.sqf:14`, 127 ms) with two ~1500 m HQ scans | Kills a map-wide candidate scan; same buildings restored from a far smaller set. Cadence already floored at 1800 s — touch only the radius | med / low-med | **trial** | performance | TRIAL on test box; must prove BOTH base areas stay covered or structures stop self-healing |
| 8 | **Live "Front-Status" Discord embed (spectacle keystone) — BUILD FIRST** — off-box RPT-tailer writes `front.json`; second embed in the bot's existing 60s loop (town score, contested town, who's pushing, last flip) | Highest reach-per-effort, **zero server risk**. Reuses the running bot + already-emitted `AICOMSTAT|FRONT` + `WASPSTAT|CAPTURE`. Turns an invisible AI-vs-AI war into a public scoreboard | high / low-med | **adopt** | spectacle | 100% off-server; sidecar JSON the bot already `LoadFromFile`s; show a "last updated" age to catch a stalled tail |
| 9 | **Off-server end-of-round LLM battle recap — BUILD SECOND** — on `ROUNDEND`, pre-aggregate CAPTURE/FRONT/STALL/HQ_STRIKE/KILL/PLAYERSTATS → Claude → Discord | Shareable artifact, off-box, zero risk; shares #8's ingest plumbing. Feed a pre-aggregated summary, NOT raw RPT (anti-hallucination) | high / med | **adopt** | spectacle | off-server; latch one recap per round/MISSINIT; Ray=834428635896610886 |
| 10 | **Fix the phantom capture counter** — drop the `CAPTURED` match (`wasp-b57-soak.ps1:51`), count real `CAPTURE` events | Confirmed false metric historically; B65/B66 soak shows WASPSTAT-captures already matching the counter — **likely already fixed; verify and close**. Harness-only | high / low | **adopt (verify-close)** | bugs-telemetry | harness-only; repoint $cap consumers in one pass |
| 11 | **[PARKED→REJECT, B59-era] Flag-then-HC-delete reclaim of alive≤1 teams** — was the B59 #1 | **Downgraded from hold to REJECT this cycle.** It deletes live units mid-combat: player-visible-vanish + conga-line + group-slot-thrash hazards (vs the 110-group ceiling), and it fixes the SYMPTOM not the cause. The add-only HCTopUp (#6) is strictly safer and attacks the root | — / — | **reject** | aicom-units | superseded by #6; if ever revisited, must be HC-local delete + no-player-900 m + non-COMBAT guard |
| 12 | **War-map timelapse (spectacle, BUILD THIRD/opportunistic)** — replay CAPTURE → PNG frames → GIF/MP4 off-box | Shareable, off-box, zero risk; shares #8/#9's CAPTURE parser. Needs a town→coord table + ffmpeg | med / med-high | **trial** | spectacle | off-server; build after #8/#9 prove the pipeline |

**Below the top 12 (valid, lower priority):** antistack_main/flush ms + restorer ms in the soak harness (performance, adopt — signal only, the cost is off-limits); TEAM_FOUNDED `class=` mislabel fix (bugs, adopt); engine-flood ("Message not sent" / "Object not found") tripwire in the harness (bugs, adopt). **PARKED-as-insurance:** STUCKSTAT spawn-wedge diag + 3-part strand fix, and spearhead re-pick / maneuver-vs-garrison gate / non-reset strand counter — fronts are MOVING through B66, build only if the front re-freezes over a full round (read-only diag portions are safe to land anytime).

---

## By facet

### aicom-units & the bleed/wedge _(MOST-IMPLICATED this cycle — the bleed has a named root cause)_
- **Fresh B66 signal: EAST issued ~11 `retreat-and-reform` orders at one tick, all alive=1.** Root cause, code-verified: **HC-founded teams have no refill path.** B57 pads infantry to found-size once at founding (`AI_Commander_Teams.sqf:363-391`); the B61 refill-at-base loop (`AI_Commander_Produce.sqf:96-237`) is **server-local-only** and never reaches HC teams (which are ~100% of the field). HC teams attrit one-way → trickle in as alive=1 → retreat → re-found from scratch.
- **Telemetry contradiction gating the fix:** retreat fires only when `_canProduce` (`:78`, requires `!wfbe_aicom_hc`), yet CMDRSTAT shows **0 srvTeams** with ~11 retreat lines. **#1 (tag + RETREATSTAT) settles it** — HC churners (→ wire #6 HCTopUp) vs mis-tagged server-local (→ propagation bug). Do this first.
- **#6 HCTopUp is the root-cause fix** (add-only, gated on #1). **#11 reclaim is REJECTED** (deletes live combat units — symptom not cause, higher risk than the bleed).
- The churn is slow and FPS-cheap (founding throttled, `Teams.sqf:156`), 0 remnants, 0 stranding — it is NOT a death spiral. Fix it at the root, don't panic-delete.

### economy & balance _(pressure-tested again this cycle — verdict UNCHANGED: change nothing, measure)_
- **Q2 (per-side asymmetry / EAST bleed): DO-NOT-TOUCH-BALANCE STILL HOLDS.** Income is `GetTownsSupply` per side (`updateresources.sqf:56`) × a **side-agnostic** `_pcMult` (`:32-46`) × time-curve — no per-side constant anywhere; the only `switch` is by income-system, not side. Bootstrap floor flows unconditionally to a 0-town side (`:103,:121`); the funds-famine fallback keeps funds flowing at the supply cap (`:108-122`); founding spends FUNDS not supply (`Teams.sqf:437-455`); the dynamic-target + banking valve (`Teams.sqf:57-100`) actively converts a *losing* side's banked cash into MORE teams. **The losing side is structurally helped, not starved.** EAST's 1 armor / 120 pers is the fair downstream of holding fewer/poorer towns → less `GetTownsSupply` → fewer teams. It is emergent, and the stronger side swaps between soaks.
- **The EAST bleed is a TACTICS symptom (piecemeal commit) of the no-HC-refill ROOT, not an economy gap.** The fix is #6 (refill in place so teams arrive at full strength), not income tuning.
- **REJECT** raising `SUPPLY_INCOME_MULT`, adding town-scaling (already exists, `updateresources.sqf:76` ×0.35), gating founding on supply, AND **"wait-to-mass" staging** (any hold-for-buddies logic is one bug from a frozen AI that won't re-wake — explicitly banned; concentration already comes from the spearhead picker, `Strategy.sqf:127`).
- **ADOPT only diagnostics: #3 (inf/veh split + per-side town-supply) and #5 (SUPPLYSTAT).** Measure for a cycle, then revisit with data. FUNDS-FALLBACK remains the intended funds→tech converter (`Server_AI_Com_Upgrade.sqf:90-95`).

### aicom-strategy & front-stall _(front MOVING through B66 — freeze fixes remain insurance)_
- B66 reality: WEST PRESS / EAST DEFEND, real flips (Lopatino), one self-corrected `SPEARHEAD_REPICK`. The B59 `contested=0` partition is NOT present. The B59 strategy fixes stay **insurance** — build only if the front re-freezes over a full round.
- **The one urgent strategy item is #2: the MHQ-reloc deploy-time enemy guard** (still open after B66 — see B60 section).
- POSTURE remains largely dead telemetry (many writers, ~1 reader) — do NOT bolt behaviour onto it.

### performance / FPS _(B66 holds; the sag is still the spawn burst)_
- B66 mid-game **31-39 at 7 towns / 433 units**; 47 quiet; HC-2 25. The only sag is the B65-observed **5-6-town simultaneous spawn burst** (`fps=15 units=530`). **ADOPT #4 (stagger activations)** — the legal, decisive lever. **TRIAL #7 (restorer per-HQ bubbles).**
- **REJECT (debated): per-HC group caps** (strand AI via dropped delegation), **body/wreck-timeout cuts** (nothing accumulating), **live group-merging** (re-homes fighting AI, no group-cap pressure), **lowering `TOWNS_ACTIVE_MAX`** (gating-off ≠ staggering). **antistack (507/526 ms) is the dominant cost but OFF-LIMITS by rule** — flatten the burst instead.
- Instrument antistack/restorer ms in the harness (signal only).

### player spectacle & AI cinematics _(most-overdue facet — verdict UNCHANGED: consume the firehose off-server)_
- A Discord bot already runs and posts a 60s status embed (`DiscordBot/GameStatusUpdater.cs`) but reads only a 4-field `database.json` and ignores the rich RPT telemetry. **Build order, all OFF-SERVER, zero live-risk:** #8 Front-Status embed FIRST → #9 LLM recap SECOND → #12 timelapse THIRD.
- **REJECT all in-mission spectacle** (front-line markers dominated by #8; director-cam collides with no-sim-gating + never-idle-AI + antistack; scripted stalemate-breakers already exist as STALL/SPEARHEAD_REPICK/WEDGE_RELEASE — good *input* to the recap, not a build).

### bugs & telemetry
- **Adopt the read-only diagnostics: #1 (retreat-tag + RETREATSTAT), #3 (inf/veh split + town-supply), #5 (SUPPLYSTAT), #10 (phantom counter verify-close), TEAM_FOUNDED `class=` mislabel fix, engine-flood tripwire.** All harness/diag-only, build-independent.
- B66 live: 0 script errors; benign noise only (`Object N not found` net-despawn races ~25×, `Cannot use magazine` loadout mismatches, HC cargo chatter). Leave the engine alone.

---

## B60 / MHQ-RELOCATION — **STILL CONDITIONAL-GO (the one deploy-time ENEMY guard survives B66)**

**B66 reconciliation (code-verified this cycle):**
- **Plan #2 (deploy-time ENEMY re-check) is STILL GENUINELY OPEN.** B66 added two MHQ hardenings, both **player/orphan-facing, not enemy:** an orphan/driverless guard on the mobilize race (`AI_Commander_MHQReloc.sqf:117-134`) and a teleport dest-clear-of-players guard (`:191-193`, the new `:193` adds dest-clear). Neither re-checks ENEMIES at the final deploy. The deploy step at `:226-238` computes `_finPos`, avoids water, then ExecVMs `Construction_HQSite.sqf` with **no enemy test** — the enemy-clear idiom exists only at trigger time (`:84-89`). **Insertion point: immediately before the ExecVM at `:238`; `_enemyClear`/`_enemySide` are already Spawn params 8-9 (`:106-107`) — no new plumbing.** Keep default-ON.
- **Heli cannon-nudge concern is now MOOT — do NOT re-propose disabling.** B66 scoped the nudge to **heli-teams-only** (`Common_RunCommanderTeam.sqf:124-131`, gated behind `_hasAttackHeli` — it no longer runs a ~7s loop on every infantry/armor team) and fixed the muzzle resolution (`:152-163`). The FPS/overhead reason to "ship only telemetry + disable" is gone. It is real engagement logic, A2-OA-safe (no disableAI, no sim-gating). Ship the air-telemetry line if desired, but **leave the nudge on.**
- **AICOM air-gate 4→3 CONFIRMED + CLOSED:** `WFBE_C_AICOM_AIR_MIN_TOWNS = 3` (`Init_CommonConstants.sqf:166`, "B66: 4->3"); the `getVariable [...,4]` call-site literals are inert fallbacks. Effective gate = 3.

**Recommended action (#2):** the surgical deploy-time enemy guard — keeps the feature, closes the only unguarded path. **REJECT** disabling (patchable) and **REJECT** decoupling the respawn anchor from `GetSideHQ` (read in dozens of places; the deploy guard mitigates the same risk far cheaper).

---

## Open questions for Ray

1. **The alive=1 churn has a named root cause: HC-founded teams have NO refill path.** Green-light landing the **read-only retreat-tag + RETREATSTAT diagnostic (#1)** this cycle to settle the telemetry contradiction (0 srvTeams vs ~11 retreat lines), then — if it confirms HC churners — **TRIAL wiring the already-written `AI_Commander_HCTopUp.DRAFT.sqf` (#6, add-only, default-OFF)?** **Recommend yes to #1 now; #6 gated on its result.**
2. **MHQ-reloc deploy-time ENEMY re-check survived B66** (B66 only added player/orphan guards). Green-light the surgical guard at `AI_Commander_MHQReloc.sqf:238` (re-use `:84-86`), keep default-ON? **Recommend yes** — one block, zero new plumbing, closes the only unguarded path.
3. **Economy: DO-NOT-TOUCH-BALANCE re-confirmed against the EAST-bleed evidence** — the bleed is the no-HC-refill root + piecemeal tactics, not an income asymmetry. OK to change NOTHING in balance and just land the **inf/veh split + per-side town-supply (#3)** and **SUPPLYSTAT (#5)** diagnostics?
4. **FPS:** still OK to trial the **town-activation stagger (#4)** on a test box (B66 holds 31-39 at 7 towns; the only sag is the 5-6-town burst)?
5. **Spectacle:** green-light building the off-server **Front-Status Discord embed (#8)** first, then the LLM recap (#9)? All off-server, zero live-server risk.
6. **Reclaim #11 (flag-then-HC-delete) is now REJECTED** in favour of the add-only HCTopUp (#6). Agree to drop it?

_(CLOSED by B66, removed from this list: the heli cannon-nudge disable question — B66 scoped it to heli-teams-only + fixed the muzzle, so it's moot; and the air-gate, confirmed at 3.)_

---

## What NOT to touch

- **The live economy — change nothing.** Income is symmetric + town-supply-keyed (no per-side constant); the bootstrap floor + funds-fallback + banking valve already backstop the losing side; FUNDS-FALLBACK is intended; town-scaling already exists; founding spends funds not supply. The EAST bleed is a TACTICS/no-refill symptom — fix #6, not balance. Measure (#3/#5) first.
- **No "wait-to-mass" / hold-for-buddies AI staging.** Any team standing idle waiting for the group is one bug from a frozen AI that won't re-wake — banned. Concentration comes from the spearhead picker (`Strategy.sqf:127`), not from holding teams back.
- **Do NOT flag-then-HC-delete live combat teams (#11 rejected).** It deletes units mid-fight (player-visible-vanish + conga-line + group-slot-thrash). Use the add-only HCTopUp (#6) instead.
- **Do NOT re-propose disabling the heli cannon-nudge** — B66 scoped it to heli-teams-only + fixed the muzzle; the concern is moot.
- **No live-server spectacle.** All adopted spectacle is OFF-SERVER (Discord embed/recap/timelapse from telemetry already emitted). No in-mission cams, markers, or scripted events.
- **antistack — hands-off entirely.** Only safe lever is the input (total AI / PC_LOW), never the algorithm. Flatten the *town-activation burst* (#4) instead.
- **Performance traps to AVOID** (debated, rejected): per-HC group caps (strand AI), body/wreck-timeout cuts (nothing accumulating), live group-merging (re-homes fighting AI), lowering `TOWNS_ACTIVE_MAX` (gating-off, not staggering).
- **Towns / per-side balance stay HARD.** Symmetric by construction; no per-side coefficient, no mission.sqm ownership edits.
- **No sim-gating, no frozen AI.** Every code-shaped item re-tasks/reclaims/refills/measures; none idles an AI.
- **Do not deploy. Do not merge this branch. Propose-only.** (Builds are deployed by Ray, not by me.)
