# WASP Improvement Plan (all facets) — LIVE BUILD: B65

_Seed v1: 2026-06-20 overnight multi-facet adversarial debate (15 agents, deeply code-verified). LIVING doc: refined every ~2h overnight. **UPDATED TO B65 2026-06-21 (cycle 5)** — live server is now B65. PROPOSE-ONLY — nothing here is deployed by me; builds are deployed by Ray._

**Changelog**
- _2026-06-21 (cycle 5 — MHQ-RELOC FIRED LIVE + FPS-at-15-teams deep-dive): live is now **B65**. Fresh read-only RPT pull. **THE LANDMINE FIRED:** `AICOMSTAT|MHQRELOC|WEST|63|TRIGGER|frontDist=3531|dest=[2416.65,6217.25,0]|back=800` → `MOBILIZED|mhq=LAV25_HQ` — the MHQ-reloc the plan flagged as "the one armed landmine" triggered for the FIRST time, at front 3531 m, and reached MOBILIZED with **no stranding, no script errors**. Code-deep adversarial review this cycle reaches **CONDITIONAL-GO, not disable:** the clean fire is a **lucky first fire** — the trigger guards are sound, but the **DEPLOY step (`AI_Commander_MHQReloc.sqf:209-227`) ships with NO enemy re-check** at the destination after a drive of up to 600 s (the enemy-clear test exists only at trigger, `:82-89`), and the respawn anchor IS coupled to the HQ (`Client_GetRespawnAvailable.sqf:8-9` → `GetSideHQ`). So the surgical fix is **add the deploy-time enemy re-check**, NOT disable the whole feature — a big de-escalation from cycle-4's "set ENABLE=false." **Second deep-dive: FPS at 15 teams.** New high-load data: **`SRVPERF|55|fps=15|units=530|groups=129|veh=60|activeTowns=6`** and `fps=16|units=466|activeTowns=5` — server sags to 15-16 once units cross ~470 and **5-6 towns activate in the SAME sweep** (spawn-burst, not steady-state). HC-2 also dips (fps=17). antistack_main/flush at 507/526 ms noted but **OFF-LIMITS**. Verdict: **ADOPT staggering town activations** (cap *new* activations per 5 s sweep in `server_town_ai.sqf` — flattens the burst, no town denied, AI still wakes on proximity); **TRIAL** shrinking the `restorer_buildings` 10.5 km `nearestObjects` (`buildings_restorer.sqf:14`, 127 ms) to per-HQ bubbles; **REJECT** per-HC group caps / body-timeout cuts / live group-merging / lowering `TOWNS_ACTIVE_MAX` (all either strand AI, churn for nothing, or gate sim). unitsPerTeam holding 6.9-7 across 15 founded teams both sides; 0 errors; 1 self-corrected wedge; 0 ASSAULT_STRANDED — B61 bleed-mitigation holds in B65._
- _2026-06-21 (cycle 4 — RECONCILE TO B61 + economy & spectacle deep-dive): live is now **B61** (`missionName=...b61`), NOT B59/B60. Fresh live pull: **FPS 47–48** (healthy, up from B59's 42), ~260 units / 3 players, **HCs absorbing 86%** of AI (DELEGSTAT 213/248 remote), **0 script errors**. **Front is MOVING, not frozen** — EAST=PRESS (myStr=99 enStr=79, holds Vybor, took NWAF, 25 fwd teams), WEST=DEFEND/BOOTSTRAP (0 towns, biasing to Solnichniy). TOWNSTAT west=0 east=1 guer=42 (early land-grab off neutral towns). **The B59 "frozen 7v7 partition" headline NO LONGER HOLDS for B61.** **unitsPerTeam=8 at warmup** (vs B59's chronic 5.5–7.7 below-floor bleed) — B61's refill-at-base appears to be MITIGATING the bleed. Only ONE `distStart=0` spawn-wedge (self-corrected via reissue); **zero ASSAULT_STRANDED**. **TWO deep debates this cycle (most-implicated + most-overdue): (3) economy/supply-tempo and (5) spectacle.** **Economy verdict: CHANGE NOTHING + measure** — the "both sides on FUNDS-FALLBACK" signal is a MISREAD; FUNDS-FALLBACK is Ray's deliberate funds→tech converter (`Server_AI_Com_Upgrade.sqf:90-95`, rate=1, set 2026-06-17), the bootstrap floor (120 sup + 100 funds/min for 2h, town-independent) already prevents a 0-town death spiral, town-scaling already exists (×0.35), and founding is paid in FUNDS not supply. Only adopt a new SUPPLYSTAT diagnostic. **Spectacle verdict: the Discord bot already runs + posts every 60s but ignores the RPT firehose** — build the off-server Front-Status embed FIRST (zero server risk, reuses already-emitted AICOMSTAT|FRONT), then the LLM recap, then a war-map timelapse; REJECT all in-mission cams/markers. **NEW TOP RISK: B61 ships the MHQ-reloc default-ON — the forbidden FORWARD-teleport — it simply hasn't FIRED yet because the front is still <2500 m from HQ (land-grab phase). When the front pushes out it WILL trigger and can drive the respawn anchor onto a hot tile.** This is now the #1 watch item._
- _2026-06-21 (cycle 3 — was the final B59 pull): sharpened the strand root cause to a terrain-blind SPAWN wedge (`distStart=0` = leader hasn't moved since order issue, `Common_RunCommanderTeam.sqf:517-519` self-documents "distStart=0 at base"). Reaffirmed asymmetry is downstream of stranding-geometry (stronger side SWAPS between soaks), not economy. NOTE: this reasoned against B59; B61 evidence (front moving, bleed mitigated, zero stranding so far) supersedes much of it — re-validate the strand items against B61 before committing._
- _2026-06-21 (06:00 CONSOLIDATION, B59): measured the retreat-and-reform death loop at 6,397 orders, 100% alive=1, all 5.8–9.7 km out. B59-specific; B61's refill-at-base may have closed this — confirm._
- _2026-06-21 (cycle 2): economy symmetric BY CONSTRUCTION; B60 MHQ build diverges from spec → HOLD/re-build._
- _2026-06-21 (cycle 1): surfaced alive=1 retreat-and-reform churn; added #1 (disband alive≤1), strand teleport._

---

## Headline

**State of B65 — healthy and moving; the landmine fired and was survivable, but it shipped without its last rail.** Live B65 is mechanically strong: **FPS 47–48 quiet, 0 script errors**, HCs absorbing the AI load, full **15 founded teams both sides** at **unitsPerTeam ~6.9–7** (B61's refill-at-base bleed-mitigation holds), captures real (phantom-counter fix working: WASPSTAT-captures == counter). The front is **MOVING, not frozen** — WEST↔EAST oscillating DEFEND/HOLD/PRESS at near-parity, real town flips (Myshkino). **Zero ASSAULT_STRANDED, one self-corrected spawn-wedge.**

**The landmine fired — and survived — but that's a lucky first fire, not validation.** The MHQ-relocation triggered live for the first time (`MHQRELOC|WEST|63|TRIGGER|frontDist=3531|...back=800` → `MOBILIZED`), reached MOBILIZED with no stranding and no errors. Code-deep review this cycle changes the verdict from cycle-4's "disable it" to **CONDITIONAL-GO**: the trigger-side guards (water, enemy-clear, single-flight) are sound, and the back-off math is correct. **But the DEPLOY step (`AI_Commander_MHQReloc.sqf:209-227`) re-checks NOTHING** — after a drive of up to 600 s it deploys the HQ (and with it the player respawn anchor, which IS coupled via `Client_GetRespawnAvailable.sqf:8-9` → `GetSideHQ`) at the destination with no fresh enemy test. The clean fire happened only because the dest tile was still clear at deploy. **The fix is a 1-block surgical guard, not killing the feature: re-run the trigger-time enemy-clear test (`:82-89`) at `_finPos` before deploy at `:221`, converging on deploy-in-place if hot.** Keep default-ON; add the rail.

**The second deep-dive — FPS at 15 teams — now has real high-load data.** The server sags to **fps 15–16 at 466–530 units when 5–6 towns activate in the same sweep** — it's a **spawn/scan burst**, not steady-state cost. The safe, decisive lever is **staggering town activations** (cap *new* activations per 5 s sweep in `server_town_ai.sqf`): no town denied, AI still wakes on proximity, the createGroup/delegation cost spreads over seconds instead of one frame. antistack (507/526 ms) is the dominant raw cost but is **off-limits by rule** — the burst is what we can legally flatten.

**This cycle's two debates (most-implicated by the fresh RPT):**
- **B60 MHQ-reloc (it FIRED):** code-deep adversarial review. The live clean fire is real but is a **lucky first fire** — the deploy step ships without the enemy re-check the trigger step has. **CONDITIONAL-GO: keep default-ON, ADD a deploy-time enemy guard** (`AI_Commander_MHQReloc.sqf:221`, re-use `:82-89`). TRIAL raising STANDOFF 800→1000–1200 and a post-deploy cooldown. **REJECT disabling** (the gap is patchable) and **REJECT decoupling respawn** (GetSideHQ is read in dozens of places; too invasive — the deploy guard mitigates the same risk far cheaper).
- **Performance / FPS at 15 teams (it sagged):** the fps-15 dip is a **5-6-town simultaneous spawn burst**, not steady-state. **ADOPT staggering town activations** (per-sweep new-activation budget in `server_town_ai.sqf`). **TRIAL** shrinking the `restorer_buildings` 10.5 km `nearestObjects` (`buildings_restorer.sqf:14`, 127 ms) to per-HQ bubbles. **REJECT** per-HC group caps (strand AI via "delegation dropped"), body/wreck-timeout cuts (`dead=13`, nothing accumulating), live group-merging (no group-cap pressure; re-homes fighting AI), and lowering `TOWNS_ACTIVE_MAX` (that's gating-off, not staggering).

---

## Top actions (all facets, ranked) — cycle-5

| # | What | Why (B65 evidence) | Impact / Effort | Verdict | Facet | A2-OA-safe pointer |
|---|------|--------------------|-----------------|---------|-------|--------------------|
| 1 | **Add a deploy-time enemy re-check to MHQ-reloc** (the surgical fix that keeps the feature) — re-run the trigger-time enemy-clear test (`AI_Commander_MHQReloc.sqf:82-89`) against `_finPos` immediately before the deploy at `:221`; if the tile is hot, deploy-in-place / redeploy backward instead of onto the front. Keep default-ON | **The landmine FIRED live (frontDist=3531→MOBILIZED) and survived only because the dest was clear at deploy.** The deploy step (`:209-227`) currently re-checks NOTHING after a ≤600 s drive, and the respawn anchor is coupled (`Client_GetRespawnAvailable.sqf:8-9`). This one block closes the only genuinely unguarded path | **highest** / low | **adopt** | aicom-strategy / B60 | re-use existing `:82-89` block; converge on a deploy (never leave a frozen/idle MHQ); no banned cmds. Optional TRIAL: STANDOFF 800→1000-1200, post-deploy cooldown |
| 2 | **Stagger town activations to flatten the FPS-15 spawn burst** — add a per-sweep budget so ≤2 *new* towns activate per 5 s pass in `server_town_ai.sqf` (~`:142-187`); the rest wake on the next sweep (their inactivity stays fresh) | **NEW high-load data: `fps=15 units=530 activeTowns=6`.** The sag is 5-6 towns dumping ~10-16 fresh groups through delegation in ONE tick — spread it over seconds. **No town denied, AI still wakes on proximity**, and it incidentally smooths the HC-2 dip for free | high / low-med | **adopt (trial on test box)** | performance | plain counter + `time` debounce (existing idiom `:155`); NOT lowering `TOWNS_ACTIVE_MAX` (that's gating-off). Verify ≤10 s defender-spawn lag is acceptable on a 3rd+ simultaneous town |
| 3 | **CONFIRM the bleed stays fixed through B65** — keep watching that unitsPerTeam holds (B65: 6.9–7 at 15 founded teams) and ASSAULT_STRANDED/retreat-and-reform stay near-zero, before spending effort on #7/#8/#9 (the B59 reclaim/top-up/strand fixes) | B65 corroborates B61: full 15-team rosters, 0 stranding, 1 self-corrected wedge. **Don't fix what refill-at-base fixed.** Gates whether the aicom-units block is still needed | high / low (read-only soak watch) | **adopt** | bugs-telemetry | observation-only; #7/#8/#9 stay PARKED pending this |
| 4 | **TRIAL: shrink `restorer_buildings` scan to per-HQ bubbles** — replace the single 10.5 km `nearestObjects` (`buildings_restorer.sqf:14`, 127 ms @ fps=14) with two ~1500 m scans around each HQ | Kills a map-wide candidate scan; same buildings restored from a far smaller set. Cadence is already throttled (1800 s floor) — **don't touch the timer**, only the radius | med / low-med | **trial** | performance | TRIAL on test box first: must prove BOTH base areas remain covered or some structures stop self-healing |
| 5 | **SUPPLYSTAT diagnostic line** — emit one periodic per-side aggregate `SUPPLYSTAT|v1|<side>|<min>|reserve=..|income=..|fallbackCount=..|towns=..`, gated to the income tick (~60s), reusing the existing `AICOMSTAT`/`TOWNSTAT` `diag_log` idiom | The economy debate's honest path: the "supply-starved" worry is a misread, but there is **no periodic per-side aggregate** to PROVE tempo is healthy. One cheap line turns the next economy debate from anecdote into data | high / low | **adopt (trial)** | economy / bugs-telemetry | `diag_log`+`str`+`+` only (no banned cmds); counter via `setVariable` at the fallback site `Server_AI_Com_Upgrade.sqf:134`; emit at income interval, NEVER per-unit/per-frame |
| 6 | **Live "Front-Status" Discord embed (spectacle keystone) — BUILD FIRST** — off-box RPT-tailer writes `front.json` next to the existing `database.json`; add a second embed to the bot's existing 60s loop showing town score, contested town, who's pushing where, last flip | **Highest reach-per-effort, zero server risk.** Reuses the already-running Discord bot + already-emitted `AICOMSTAT|FRONT` + `WASPSTAT|CAPTURE`. Turns an invisible AI-vs-AI war into a public scoreboard for the whole Discord, not 3 in-game humans. **No new in-mission emission needed** | high / low-med | **adopt** | spectacle | 100% off-server; lowest-touch = sidecar JSON the bot already `LoadFromFile`s. Show a "last updated" age to catch a stalled tail |
| 7 | **Off-server end-of-round LLM battle recap — BUILD SECOND** — on `ROUNDEND`, pre-aggregate the round's CAPTURE/FRONT/STALL/HQ_STRIKE/KILL/PLAYERSTATS into a compact structured summary → Claude → Discord. Fold hero/blooper beats in as a section | Shareable artifact, off-box, zero risk; shares the RPT-ingest plumbing stood up for #6. Feed the model a pre-aggregated summary (towns flipped, net front delta, biggest fight, top human), NOT raw RPT — anti-hallucination | high / med | **adopt** | spectacle | off-server; latch one recap per round/MISSINIT; Ray=834428635896610886. Optional later: one gated `WASPSTAT|ROUNDSTART` line to make round-slicing unambiguous |
| 8 | **[PARKED, B59-era] Reclaim alive≤1 teams far from HQ instead of perpetual retreat** — flag-then-HC-delete so a full-size team re-founds | Was the B59 #1 (6,397 alive=1 retreat orders). **Gated behind #3** — refill-at-base appears to sustain unitsPerTeam through B65; do not build until #3 shows the bleed persists | high / low | **hold (re-validate)** | aicom-units | flag-then-HC-delete (server `deleteVehicle` can't reach HC-local units); re-found cooldown to avoid thrash |
| 9 | **[PARKED, B59-era] STUCKSTAT spawn-wedge diagnostics + 3-part strand fix** (#2a/#2 from B59) | B65 shows only 1 self-corrected wedge, 0 stranding. **Gated behind #3.** Keep the diagnostics ready; defer the spawn-relocation fix unless the wedge recurs over a full round | high / low (diag) | **hold (re-validate)** | aicom-units / bugs | read-only `diag_log` additions are still safe to land anytime; the spawn road-snap carries conga-line risk — gate it |
| 10 | **[PARKED, B59-era] Re-pick spearhead on stall + maneuver-vs-garrison gate + non-reset strand counter** (B59 #3/#4/#5) | The B59 frozen-partition fixes. **B61/B65 fronts are moving**, so these are no longer urgent — keep as insurance if the front re-freezes over a full round | high / med | **hold (re-validate)** | aicom-strategy | selection-only; reuse blacklist idiom `AssignTowns.sqf:208-219` |
| 11 | **Fix the phantom capture counter** — drop the `CAPTURED` match (`wasp-b57-soak.ps1:51`), count real `CAPTURE` events, relabel "flips" | Confirmed false metric (raw grep 352 vs ~12 real events). Harness-only, build-independent. **B65 soak shows WASPSTAT-captures already matching the counter — likely already fixed; verify and close** | high / low | **adopt (verify-close)** | bugs-telemetry | harness-only; repoint $cap consumers in one pass |
| 12 | **War-map timelapse (spectacle, BUILD THIRD/opportunistic)** — replay CAPTURE lines into PNG frames → GIF/MP4 off-box, reusing the bot's CHERNARUS map data | Shareable, off-box, zero risk; shares #6/#7's CAPTURE parser. Needs a town-name→coordinate table + ffmpeg step | med / med-high | **trial** | spectacle | off-server; build after #6/#7 prove the ingest pipeline |

**Below the top 12 (valid, lower priority):** antistack_main/flush ms in the soak harness (performance, adopt — instrumentation only, the cost is off-limits but the *signal* isn't); infPerTeam/vehPerTeam split diagnostic (bugs, adopt); TEAM_FOUNDED `class=` mislabel fix (bugs, adopt); engine-flood ("Message not sent" / "Object not found" 63× benign) tripwire in the harness (bugs, adopt); HC-team live top-up (aicom-units, trial — only if #3 shows the bleed persists).

---

## By facet

### economy & balance _(most-implicated by fresh data this cycle — verdict: change nothing, measure)_
- **The "both sides supply-starved" reading is a MISREAD — debated code-deep this cycle.** FUNDS-FALLBACK is **not** a famine alarm; it is Ray's deliberate funds→tech converter (`Server_AI_Com_Upgrade.sqf:90-95`, `WFBE_C_AICOM_UPGRADE_FUNDS_RATE=1`, set 2026-06-17). It fires only when a side wants a vehicle-tier upgrade, has dry supply, and pays the supply price as a funds surcharge. **Every fallback line in the RPT = a tech tier that DID unlock**, not a denied purchase. The feature working IS the telemetry.
- **No 0-town death spiral exists.** The bootstrap stipend (`AI_Commander.sqf:207-247`, constants `Init_CommonConstants.sqf:248-250`) gives a 0-town side **120 supply/min + 100 funds/min for up to 2 h, town-count-independent**, plus a BOOTSTRAP_BIAS picker aiming it at the nearest cheap town. WEST at 0 towns is on the floor BY DESIGN (land-grab phase), not spiralling — which is why it still affords its id6→L1 upgrade via fallback.
- **Town-scaling already exists** (`updateresources.sqf:76` income = `round(townSupplyValue × 0.35)`; `Common_GetTownsSupply.sqf` sums owned towns). Adding MORE town-scaling would double-count and STEEPEN the rich-get-richer snowball — the opposite of anti-snowball. **Reject.**
- **Founding spends FUNDS, not supply** (no `ChangeSideSupply` on any founding path; spend lives in `ChangeAICommanderFunds`/`ChangeTeamFunds`). So "cap founding to match supply rate" is a category error — and capping founding risks idle/deferred AI (antistack hazard). **Reject.**
- **Raising `SUPPLY_INCOME_MULT` (0.35→higher): REJECT.** It reverts a 1-cycle-old intentional throttle ("so the AI earns progression from towns+convoys instead of drowning in supply", `Init_CommonConstants.sqf:416`), funds more base structures (not units), and BLINDS the fallback diagnostic. Low effort, negative impact.
- **Adopt: #3 SUPPLYSTAT diagnostic only.** Measure tempo for a cycle, then revisit with data instead of anecdote. The honest bottom line: **change nothing in the economy yet.**
- **DO-NOT-TOUCH-BALANCE stands** (re-verified across prior cycles: flat 200000 both sides, symmetric coefficients, stronger side swaps between soaks).

### player spectacle & AI cinematics _(most-overdue facet — verdict: consume the firehose off-server)_
- **Key infra fact (verified): a Discord bot already runs and posts a status embed every 60s** (`DiscordBot/GameStatusUpdater.cs`) — but it reads only a 4-field `database.json` (scores/world/uptime/playercount) and **ignores the rich RPT telemetry** (WASPSTAT KILL/CAPTURE/ROUNDEND; AICOMSTAT FRONT/POSTURE/STALL/HQ_STRIKE/MHQRELOC/TEAM_FOUNDED — all timestamped + side-tagged). The WASPSTAT doc itself flags "DiscordBot tails the RPT" as aspirational.
- **Build order (all OFF-SERVER, zero live-risk, all buildable from telemetry ALREADY emitted):**
  1. **#4 Front-Status embed (FIRST)** — the keystone. Reuses the running bot + `AICOMSTAT|FRONT` + `CAPTURE`. Lowest-touch path: an off-box tailer writes `front.json`; the bot adds a second embed in its existing loop. Converts an invisible war into a public scoreboard. **Highest reach-per-effort in the whole plan.**
  2. **#5 End-of-round LLM recap (SECOND)** — triggered by `ROUNDEND`, pre-aggregate then narrate. Fold hero/blooper (KILL lines) in as a section.
  3. **#10 War-map timelapse (THIRD/trial)** — replay CAPTURE lines off-box.
- **REJECT all in-mission spectacle:** front-line map markers (strictly dominated by #4 — same data, more reach, zero risk); auto director-cam (highest risk: spectator SQF + distance/sim reasoning to find the hot fight collides with no-sim-gating + never-idle-AI + antistack constraints, ~0 reach for 3 humans — a human capturing a stream can free-cam manually); stalemate-breaker scripted event (that's a war-balance mechanic, and the breakers already exist — STALL/SPEARHEAD_REPICK/WEDGE_RELEASE; it's good *input* to the recap, not a spectacle build).

### aicom-strategy & front-stall _(B61: front is MOVING — B59 freeze fixes demoted to insurance)_
- **B61/B65 reality:** sides oscillate DEFEND/HOLD/PRESS at near-parity, real town flips (Myshkino), front advancing — the B59 `contested=0` 7v7 partition is NOT present. The B59 strategy fixes (#10: spearhead re-pick, maneuver-vs-garrison gate, non-reset strand counter) are **demoted to insurance** — keep ready, build only if the front re-freezes over a full round.
- **The one urgent strategy item is #1: the MHQ-reloc deploy-time guard** (see B60 section — it FIRED this cycle; add the guard, keep the feature).
- POSTURE remains largely dead telemetry (many writers, ~1 reader) — do NOT bolt behaviour onto it.

### aicom-units & the bleed/wedge _(B61: appears mitigated — re-validate before building)_
- **B65 unitsPerTeam ~6.9–7 across 15 founded teams both sides** (vs B59's chronic 5.5–7.7 *below floor*), **1 self-corrected spawn-wedge, 0 ASSAULT_STRANDED.** Refill-at-base looks to have closed the B59 death-loop and spawn-wedge through two builds now. **#3 (confirm over a full round) gates the entire block.**
- The B59 reclaim (#8), STUCKSTAT diag + 3-part strand fix (#9) stay PARKED pending #3. The read-only STUCKSTAT diagnostics are safe to land anytime if you want the signature captured cheaply; the spawn road-snap keeps its conga-line risk and stays gated.
- Every parked item still re-tasks/reclaims — none freezes an AI. Sequencing rule preserved: HC-local units must be flagged-then-HC-deleted (server `deleteVehicle` can't reach them).

### performance / FPS _(DEEP-DIVED this cycle — the sag is a spawn burst, and it's legally flattenable)_
- **New high-load data: `SRVPERF|55|fps=15|units=530|groups=129|veh=60|activeTowns=6` and `fps=16|units=466|activeTowns=5`.** Quiet FPS is 47; the sag appears only when units cross ~470 AND **5-6 towns activate in the SAME `server_town_ai` sweep**. It is a **spawn/scan burst (createGroup + HC delegation in one tick), not steady-state cost** — which is exactly what we can legally smooth without gating sim or freezing AI.
- **ADOPT #2: stagger town activations** — per-sweep new-activation budget in `server_town_ai.sqf` (~`:142-187`). The cap is already 12 so it isn't denying towns; the fix is to spread the *simultaneous* activations across consecutive 5 s sweeps. No town denied, AI still wakes on proximity. Highest-value/lowest-risk perf lever. *Trial on a test box; verify a 3rd+ simultaneous town's defenders spawn within ~10 s.*
- **TRIAL #4: shrink the `restorer_buildings` 10.5 km `nearestObjects`** (`buildings_restorer.sqf:14`, 127 ms @ fps=14) to per-HQ bubbles. Cadence is already floored at 1800 s — touch only the radius, and prove both base areas stay covered.
- **REJECT (tempting traps, all debated): per-HC group cap** (hits the "delegation dropped" path → strands AI un-spawned); **cutting body/wreck timeouts** (`dead=13` — nothing accumulating; risks the netcode "Object not found" flood); **live group-merging** (129 groups < 130 warn line — no pressure; re-homes fighting AI); **lowering `TOWNS_ACTIVE_MAX`** (that's gating-off, not staggering — #2 is the legal equivalent).
- **Adopt: instrument antistack_main/flush ms + restorer ms in the soak harness** (signal only — the antistack *cost* (507/526 ms) is the single biggest line but is OFF-LIMITS by rule; we still want it on the dashboard).
- **PC_LOW=12 vs 15 A/B stays deferred** until a full round is captured at the high-unit spike. Total AI is the only safe antistack lever. **antistack algorithm itself: hands-off entirely.**

### bugs & telemetry
- **Adopt #5 (SUPPLYSTAT), #11 (phantom capture counter — B65 soak shows it already matching; verify+close), infPerTeam/vehPerTeam split, TEAM_FOUNDED `class=` mislabel fix, engine-flood tripwire in the harness.** All harness/diag-only, build-independent.
- B65 live: 0 script errors; benign noise only — `Object X not found` 63× (one despawned net object 5:1443 referenced 13×), HC identity spam, ACR-DLC-absent class skips. Leave the engine alone.

---

## B60 / MHQ-RELOCATION — **FIRED LIVE, SURVIVED, CONDITIONAL-GO (add one deploy-time guard)**

**Status change this cycle:** the landmine **fired** — `MHQRELOC|WEST|63|TRIGGER|frontDist=3531|dest=[2416.65,6217.25,0]|back=800` → `MOBILIZED|mhq=LAV25_HQ`, reaching MOBILIZED with **no stranding, no errors**. Code-deep adversarial review reaches **CONDITIONAL-GO, a de-escalation from cycle-4's "disable."** The clean fire is a **lucky first fire**, not validation of the dangerous path.

**What the review actually found (file:line):**
- **Trigger guards are SOUND.** Fires at `_hq distance _frontPos > 2500` (`AI_Commander_MHQReloc.sqf:72`), re-evaluated every 180 s (`AI_Commander.sqf:158`). Enemy-clear + water guarded at trigger (`:82-89`, `:83`). Back-off math is correct: STANDOFF=800 m back toward old HQ, clamped to `_d-1` (`:74-80`) — confirmed `back=800`.
- **The respawn anchor IS coupled** (not decoupled): `Client_GetRespawnAvailable.sqf:8-9` resolves `GetSideHQ` fresh per death; `Construction_HQSite.sqf` re-deploys the static HQ at the new pos. So players respawn ~800 m behind the (contested) front town after a relocation.
- **THE ONE REAL GAP — the DEPLOY step re-checks nothing.** `:209-227` deploys the HQ at `_finPos` after a drive of up to 600 s with **NO fresh enemy test** (the enemy-clear test exists only at trigger). If an enemy closed on the dest during the drive, the soft MHQ + respawn anchor deploy onto a hot tile. The live fire avoided this only by luck.
- `setPos` on the networked LAV25_HQ (`:180`) is gated on no-player-1200 m (`:174-189`) and routed through the canonical JIP-safe `Construction_HQSite.sqf` toggle — acceptable. Oscillation is geometry-damped (after deploy the new HQ is ~800 m behind front, so dist≈800<2500 won't immediately re-fire) (`:38,92`).

**Recommended action this cycle (#1 in the table) — surgical, keeps the feature:** **re-run the trigger-time enemy-clear block (`:82-89`) against `_finPos` immediately before the deploy at `:221`; if the tile is hot, deploy-in-place / redeploy backward** (never release a frozen/idle MHQ — always converge on a deploy). This closes the only genuinely unguarded path. Optionally **TRIAL** STANDOFF 800→1000-1200 (`Init_CommonConstants.sqf`) and a post-deploy cooldown (belt-and-suspenders anti-oscillation).

**REJECTED alternatives (debated):** setting `ENABLE=false` (the gap is patchable — don't scrap a working feature); decoupling the respawn anchor from `GetSideHQ` (read in dozens of places — build menu/factories/markers; high JIP/locality blast radius for a one-armed feature — the deploy guard mitigates the same risk far cheaper). Earlier "no hop cap / no pre-factory gate" worries are **downgraded**: the back-off geometry already bounds the hop and the live fire showed the trigger behaves; the deploy-time enemy guard is the single change that matters.

**Heli cannon-nudge — REJECT as premised** (also default-ON). It rides `flyInHeight`; the attack Mi-24_P already scores kills unaided; `doFire`+`selectWeapon` spam fights the gunner FSM and dropping to 35 m near enemy hands a gunship to MANPADS — plausibly negative EV. Ship only the decoupled air-telemetry line. **Watch the B65 RPT for any sign the nudge is degrading Mi-24 survivability.**

---

## Open questions for Ray

1. **MHQ-reloc FIRED LIVE this cycle (frontDist=3531→MOBILIZED, no harm).** New verdict is CONDITIONAL-GO, NOT disable. Green-light the surgical fix — **add a deploy-time enemy re-check at `AI_Commander_MHQReloc.sqf:221` (re-use `:82-89`)** — and keep the feature default-ON? (Optional: STANDOFF 800→1000-1200.) **Recommend yes** — one block closes the only unguarded path; the clean fire was lucky, not proof.
2. **FPS sags to 15 at 530 units / 6 simultaneous town activations.** Green-light trialling the **town-activation stagger** (#2) on a test box — cap new activations per 5 s sweep, no town denied, AI still wakes on proximity? This is the cleanest legal FPS lever (antistack is off-limits).
3. **Bleed:** B65 still shows full 15-team rosters / 0 stranding — OK to keep the reclaim/strand fixes (#8/#9/#10) PARKED pending a full-round confirmation?
4. **Economy:** agree to change NOTHING and just land the SUPPLYSTAT diagnostic (#5) to measure tempo for a cycle? (The "supply-starved" reading was a misread of intended FUNDS-FALLBACK telemetry.)
5. **Spectacle:** green-light building the off-server Front-Status Discord embed (#6) first — reusing the bot that already runs — then the LLM recap (#7)? All off-server, zero live-server risk.
6. **Heli cannon-nudge is also live default-ON** — ship only the air-telemetry line and disable the nudge?

---

## What NOT to touch

- **The live economy — change nothing.** FUNDS-FALLBACK is intended (funds→tech converter); the bootstrap floor prevents a death spiral; town-scaling already exists; founding spends funds not supply. Raising `SUPPLY_INCOME_MULT`, adding town-scaling, or gating founding on supply all revert intentional tunes, risk snowball, or hit antistack. Measure (#3) first.
- **No live-server spectacle.** All adopted spectacle is OFF-SERVER (Discord embed/recap/timelapse from telemetry already emitted). No in-mission cams, markers, or scripted events — they cost FPS we must not break and reach ~3 people.
- **antistack — hands-off entirely.** Only safe lever is the input (total AI / PC_LOW), never the algorithm. (It is the 507/526 ms dominant cost but is off-limits — flatten the *town-activation burst* instead, #2.)
- **Performance traps to AVOID** (debated, rejected): per-HC group caps (strand AI), body/wreck-timeout cuts (nothing's accumulating), live group-merging (re-homes fighting AI), lowering `TOWNS_ACTIVE_MAX` (gating-off, not staggering). The only adopted FPS change is the *stagger* (#2, deferral not denial) and the restorer-radius trial (#4).
- **Towns / per-side balance stay HARD.** Symmetric by construction (flat income, symmetric templates, stronger side swaps between soaks). No per-side coefficient, no mission.sqm ownership edits.
- **No sim-gating, no frozen AI.** Every code-shaped item re-tasks/reclaims/measures; none idles an AI.
- **Do not deploy. Do not merge this branch. Propose-only.** (B61 was deployed by Ray, not by me.)
