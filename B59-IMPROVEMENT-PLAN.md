# WASP Improvement Plan (all facets) — LIVE BUILD: B66

_Seed v1: 2026-06-20 overnight multi-facet adversarial debate (15 agents, deeply code-verified). LIVING doc: refined every ~2h overnight. **UPDATED TO B66 2026-06-21 (cycle 7)** — live server is B66 (own-side MARKER fix series; gameplay unchanged from B59-B61). PROPOSE-ONLY — nothing here is deployed by me; builds are deployed by Ray._

**Changelog**
- _2026-06-21 (cycle 7 — TWO live stuck-team pathologies + the bleed is now hard-measured 2:1): live is **B66**. Fresh RPT (ticks 154-166): **srvFPS 33-47** (33 at units=413-473 / 4-5 towns; 41-48 quiet), **HC-2 dipped to 17 once** at the units=413 peak; **0 script errors in mission code** (1 external `asr_ai\...\fnc_fired.sqf:118` — a mod, not ours); **MHQRELOC fired CLEANLY twice** (TRIGGER frontDist=2850 → MOBILIZED LAV25_HQ → DEPLOYED reason=arrive, no stranding/errors). WEST PRESS holds 5 towns / EAST DEFEND 1; two self-corrected SPEARHEAD_REPICK (Kabanino→Grishino→Petrovka). **TWO most-implicated debates this cycle, both firing LIVE:** **(2) the stuck-team pathologies** and **(2/3) the bleed**. KEY NEW DISCOVERIES: **(i) the "~11 retreat orders" last cycle was a MISREAD — it is ONE team (EAST `O 1-2-F`) in an INFINITE retreat-and-reform loop**, identical `alive=1, dist=5566.83` re-issued ~per 45 s PRODUCE cycle for 30+ ticks, never resolving; the live B66 retreat path has **zero termination** (`AI_Commander_Produce.sqf` retreat block, `git show HEAD`). It fires DESPITE the `wfbe_aicom_hc` gate (`:69`) while CMDRSTAT shows `srvTeams=0` → the looping team is an **HC team leaking through the gate** (re-adopted orphan whose HC flag reads false), not a server-local team. **(ii) An UNCOMMITTED B67 retreat-cull already sits in the working tree** (`Produce.sqf` is `M`); it is **UNSAFE as-is** — its "server-local non-HC" safety comment is contradicted by the RPT, so its server-side `deleteVehicle`/`deleteGroup` would orphan HC-local units (frozen-AI rule break) and it lacks a `behaviour!="COMBAT"` guard. DO NOT commit it unchanged. **(iii) WEST `B 1-2-F` is a vehicle (crew-led) SPAWN-WEDGE strand** (`distStart=0 moved=0`, 5 STUCKSTAT strikes → 5 UNSTUCK tiers → `ASSAULT_STRANDED elapsed=483` → TARGET_ABANDON → re-dispatch). Root cost: tiers 1-2 (velocity-nudge + road-resnap) are **useless against a base wedge**; only tier-3 teleport frees it, and each strike needs another `STUCK_SECS=210` window → ~483 s wasted = latency to the only tier that works. **(iv) `vehPerTeam=0/vehTeams=0` ALWAYS is a real telemetry bug:** HC teams never get `wfbe_teamtype` set (`Common_RunCommanderTeam.sqf:66`) → `_tt=-1` → classifier guard `AI_Commander.sqf:407` is always false → every HC team (incl. crew teams) is bucketed as infantry. **The same `_tt=-1` trap breaks the HCTopUp draft's armour guard** → fixing the classifier is now a PREREQUISITE for #6. **(v) The bleed is hard-measured as a SUSTAINED 2:1:** EAST `unitsPerTeam` 4.3→4.8→4.1→3.9→3.9 vs WEST 6.2→7.9→7.6→8.9→9.0 across the soak; teams found at 10, drift to 4-9 — attrition outpaces refill on BOTH sides, far worse on the loser. Refill root re-confirmed: the Produce refill loop is server-local-only (`:62-68`), unreachable for the 100%-HC field. **Economy DO-NOT-TOUCH re-confirmed** with one correction: the banking valve (`AI_Commander_Teams.sqf:57-100`) **amplifies the WINNER** (wealth→teams, wealth∝towns) — it does NOT preferentially rescue the loser; only the bootstrap floor + funds-fallback keep a 0-town side breathing. The 2:1 is an emergent positive-feedback snowball, not an income asymmetry — fix it at refill (#6), not balance._
- _2026-06-21 (cycle 6 — RE-ANCHOR TO B66 + aicom-units bleed/asymmetry): re-anchored to B66; air-gate 4→3 CONFIRMED+CLOSED; heli cannon-nudge concern MOOT (B66 scoped to heli-teams-only + muzzle fix); MHQ deploy-time ENEMY re-check kept open; surfaced the alive=1 churn + HC-no-refill root + the (then-suspected) srvTeams telemetry contradiction. Cycle-7 RESOLVED that contradiction: it's an HC team leaking the gate, not a counter bug._
- _2026-06-21 (cycle 5 — MHQ-RELOC FIRED LIVE + FPS-at-15-teams): MHQRELOC TRIGGER→MOBILIZED first fire; FPS=15 at units=530/6 towns = spawn burst → adopt stagger. Cycle-7 re-confirms MHQRELOC firing cleanly (now full TRIGGER→MOBILIZED→DEPLOYED cycles, still no enemy re-check at deploy)._
- _2026-06-21 (cycle 4 — RECONCILE TO B61 + economy & spectacle): front MOVING not frozen → B59 freeze fixes demoted to insurance; Economy = change nothing + measure; Spectacle = consume the RPT off-server._
- _2026-06-21 (cycles 1-3): surfaced the alive=1 retreat churn + strand spawn-wedge (distStart=0); economy symmetric by construction. Superseded by B61+/B66 evidence._

---

## Headline

**State of B66 — mechanically healthy, but TWO distinct stuck-team pathologies are firing live, and the bleed is now a measured 2:1 snowball.** srvFPS 33-47 (33 at peak load, HC-2 touched 17 once), 0 mission-code script errors, MHQRELOC firing cleanly through full TRIGGER→MOBILIZED→DEPLOYED cycles with no stranding. The front is MOVING (WEST 5 towns / EAST 1, two self-corrected spearhead re-picks). Markers (B63-B66) confirmed working by Ray — not this debate's focus.

**The fresh signal #1 — the "retreat batch" was one zombie, and it loops forever.** Last cycle's "~11 retreat orders" is corrected: it is **ONE EAST team, `O 1-2-F`, in an infinite `retreat-and-reform` loop** — identical `alive=1, dist=5566.83` re-issued every ~45 s PRODUCE cycle for 30+ ticks, never closing. The live B66 retreat path has **no termination, no escalation, no cull**. It fires despite the `wfbe_aicom_hc` exclusion gate while `srvTeams=0`, which means the looping team is an **HC team whose HC-flag reads false** (a re-adopted orphan) — so it slips into the server-local retreat branch that was never meant to hold it, and stays there forever, burning an AICOM slot. **A B67 retreat-cull is already drafted in the working tree but is UNSAFE as-is** (it would `deleteVehicle` HC-local units server-side → orphan/frozen-AI; no COMBAT guard). Do not ship it unchanged.

**The fresh signal #2 — the spawn-wedge strand wastes ~483 s because the cheap tiers can't help it.** WEST `B 1-2-F` (a crew-led vehicle team) wedged at spawn (`distStart=0 moved=0`), climbed 5 STUCKSTAT strikes / 5 UNSTUCK tiers, then `ASSAULT_STRANDED elapsed=483` → abandon → re-dispatch. The recovery WORKS, but tiers 1-2 (velocity-nudge, road-resnap) are useless against a base wedge; only the tier-3 teleport frees it, and each strike costs another `STUCK_SECS=210` window. **`distStart≈0` is unambiguous proof of a base wedge — short-circuit straight to tier-3 on the first strike and you reclaim ~420 s and 4 strikes per wedge** using an existing, already-player-guarded teleport. This is the cleanest, highest-confidence code win this cycle.

**The fresh signal #3 — the bleed is a measured 2:1 snowball, and a telemetry bug was hiding part of it.** EAST `unitsPerTeam` sits at ~4 against WEST's ~8 across the whole soak; teams found at 10 and drift down because the Produce refill loop is server-local-only and never reaches the 100%-HC field. Meanwhile `vehTeams=0` is a **real bug**: HC teams never get `wfbe_teamtype` set, so the CMDRSTAT classifier buckets every team (crew teams included) as infantry — and that **same `_tt=-1` trap breaks the HCTopUp draft's armour guard**, so fixing the classifier is now a prerequisite for the refill fix. **Economy DO-NOT-TOUCH holds** (income symmetric; the banking valve actually amplifies the winner) — the 2:1 is an emergent snowball, fixable at refill, not balance.

**This cycle's two debates (most-implicated by the fresh RPT):**
- **(2) stuck-team pathologies:** ADOPT the `distStart≈0 → tier≥3` strand short-circuit (#1) and the retreat-line `hc/founded/local` tag + RETREATSTAT (#3); FLAG the uncommitted B67 retreat-cull as HARDEN-OR-HOLD (#5, must become an HC-aware disband-flag + COMBAT guard); REJECT the spawn-point relocation (too map-specific).
- **(2/3) the bleed:** ADOPT the vehTeams classifier fix (#2, telemetry + unblocks #6); TRIAL the HCTopUp refill (#6) gated on #2 + writing its missing caller AND consumer; DO-NOT-TOUCH-BALANCE confirmed.

---

## Top actions (all facets, ranked) — cycle-7

| # | What | Why (B66 live evidence) | Impact / Effort | Verdict | Facet | A2-OA-safe pointer |
|---|------|--------------------------|-----------------|---------|-------|--------------------|
| 1 | **`distStart≈0` → force tier≥3 on the FIRST strand strike** — in `AI_Commander_AssignTowns.sqf` ~`:199-201`, when `(_ldr distance (_ord select 2)) < ~10` (true base wedge), set the carried strike tier to `max(3,_strk)` so the order broadcast (`:407`) ships tier≥3 and `Common_RunCommanderTeam.sqf:528/551` teleports-to-road-node on the first fire instead of the third | **Pathology 2 is FIRING:** `B 1-2-F` burned `elapsed=483` + 5 strikes because tiers 1-2 (velocity-nudge, road-resnap) cannot clear a base wedge — `distStart=0` proves it never left spawn. Short-circuit reclaims ~420 s + 4 strikes per wedge using the **existing, already-player-guarded** teleport | **highest** / low | **adopt** | aicom-units / strategy | reuse `:528/:551` teleport; key on existing breadcrumb `_ord select 2`; emit tier atomically as order-elem-3 (`:407`); no banned cmds; team still re-wakes/re-dispatches |
| 2 | **Fix the `vehTeams=0` classifier — classify from LIVE units, not the always-unset `wfbe_teamtype`** — at `AI_Commander.sqf:401-415`, drop the `_tt>=0` guard dependence; detect a vehicle team by scanning `units _team` for a non-transport vehicle (`vehicle _x != _x` / `_x isKindOf "Tank"` / non-transport heli) | HC teams never get `wfbe_teamtype` set (`Common_RunCommanderTeam.sqf:66`) → `_tt=-1` → guard `:407` always false → **every** team bucketed as infantry → `vehTeams=0` forever. Corrupts every soak read AND **breaks the HCTopUp armour guard** (would pad crew teams with riflemen). Read-only telemetry; **prerequisite for #6** | **highest** / low | **adopt** | bugs-telemetry / aicom-units | `isKindOf`/`vehicle`/`count` only; per-unit scan on the CMDRSTAT tick (~300 s), negligible; cosmetic-only risk |
| 3 | **Tag the retreat-and-reform log + per-side RETREATSTAT (read-only)** — extend the retreat emit (live `Produce.sqf` retreat block) with `hc=%`, `founded=%`, `local=%` (via `GroupGetBool` + `local (leader)`); emit one `RETREATSTAT|<side>|count=..` after the `forEach _teams` | Settles WHICH team loops: the RPT shows `O 1-2-F` retreating with `srvTeams=0` — i.e. an HC team leaking the `:69` gate, not a server-local team. This line PROVES it, and it is the **safety input #5's cull hinges on** (HC vs server-local decides whether a server-side delete is legal) | high / very-low | **adopt** | bugs-telemetry / aicom-units | `diag_log`+`str`+`+`; all operands in scope at the emit; paced by the 45 s PRODUCE cycle, never per-frame |
| 4 | **Add a deploy-time ENEMY re-check to MHQ-reloc** — re-run the trigger-time enemy-clear idiom (`AI_Commander_MHQReloc.sqf:84-86`) against `_finPos` immediately before the deploy ExecVM at **`:238`**; if hot, deploy-in-place / redeploy backward. Keep default-ON | MHQRELOC fired CLEANLY ×2 this window (full TRIGGER→MOBILIZED→DEPLOYED, no stranding) — feature is **stable**, but it still deploys after a ≤600 s drive with **no enemy test** (it survived only because the tile was clear). B66's new guards are player/orphan-facing, not enemy. `_enemyClear`/`_enemySide` already in Spawn scope (`:106-107`) — zero new plumbing | high / low | **adopt** | strategy / B60 | re-use `:84-86`; always converge on a deploy (never frozen); no banned cmds. Optional TRIAL: STANDOFF 800→1000-1200 |
| 5 | **HARDEN-OR-HOLD the uncommitted B67 retreat-cull** (`Produce.sqf` working-tree draft, `:90-143`) — do NOT commit as-is. Rewrite so an HC team sets a `wfbe_aicom_disband` flag for the **HC to delete locally** (reuse the proven pattern at `Common_RunCommanderTeam.sqf:456-472` with its safe-distance + not-in-combat guard), and add a `behaviour (leader) != "COMBAT"` guard to the no-progress accrual | The draft's safety comment ("server-local non-HC") is **contradicted by the live RPT** (`srvTeams=0` while `O 1-2-F` loops → it's HC). Its server-side `deleteVehicle`/`deleteGroup` on HC-local units would orphan them → **frozen AI that won't re-wake (HARD-RULE break)**; and with no COMBAT guard it can reap a survivor that's alive=1 because it's *fighting* | high / med | **hold→harden** | aicom-units | HC-local delete only (never server-side delete of HC units); COMBAT guard; gated on #3 confirming HC |
| 6 | **TRIAL: wire the `AI_Commander_HCTopUp.DRAFT.sqf` refill (add-only, default-OFF)** — write the **missing caller** (`AI_Commander.sqf`) AND the **missing consumer** (`HandleSpecial` `"aicom-team-topup"` case); the worker exists (166 lines) and is add-only (`createUnit`+`joinSilent`, `:154-155`), gated by `WFBE_C_AICOM_HC_TOPUP_ENABLE` (`:44`), COMBAT-guard (`:82`), near-supply (`:84-87`), 1-team/call (`:62-95`) | **The root-cause fix for the 2:1 bleed:** HC teams have no refill (Produce loop is server-local-only, `:62-68`), so found-at-10 teams drift to ~4. Refilling in place lifts the floor on BOTH sides equally (balance-neutral). **GATED on #2** (its armour guard is broken by the same `_tt=-1` trap) and on writing both missing halves | high / med | **trial (gated on #2)** | aicom-units | add-only (no delete/leader churn); `createUnit`+`joinSilent` are 1.64-safe; watch funds-vs-founding contention (`:129`) |
| 7 | **Stagger town activations to flatten the FPS spawn burst** — per-sweep budget so ≤2 *new* towns activate per 5 s pass in `server_town_ai.sqf` (~`:142-187`) | B66 holds 33-47 at 4-5 towns; the only sag is the B65-observed 5-6-town simultaneous spawn burst (`fps=15 units=530`), plus HC-2 touching 17 at the units=413 peak this cycle. No town denied, AI still wakes on proximity | high / low-med | **adopt (trial on test box)** | performance | plain counter + `time` debounce (`:155`); NOT lowering `TOWNS_ACTIVE_MAX`; verify ≤10 s defender-spawn lag |
| 8 | **Live "Front-Status" Discord embed (spectacle keystone) — BUILD FIRST** — off-box RPT-tailer writes `front.json`; second embed in the bot's existing 60 s loop (town score, contested town, who's pushing, last flip) | Highest reach-per-effort, **zero server risk**. Reuses the running bot + already-emitted `AICOMSTAT|FRONT` + `WASPSTAT|CAPTURE`. Turns an invisible AI-vs-AI war into a public scoreboard | high / low-med | **adopt** | spectacle | 100% off-server; show a "last updated" age to catch a stalled tail |
| 9 | **Off-server end-of-round LLM battle recap — BUILD SECOND** — on `ROUNDEND`, pre-aggregate CAPTURE/FRONT/STALL/HQ_STRIKE/KILL/PLAYERSTATS → Claude → Discord | Shareable artifact, off-box, zero risk; shares #8's ingest. Feed a pre-aggregated summary, NOT raw RPT (anti-hallucination) | high / med | **adopt** | spectacle | off-server; latch one recap per round/MISSINIT |
| 10 | **SUPPLYSTAT diagnostic line** — periodic per-side `SUPPLYSTAT|v1|<side>|<min>|reserve=..|income=..|fallbackCount=..|towns=..`, gated to the income tick (~60 s) | No periodic per-side aggregate exists to PROVE tempo is healthy; turns the next economy debate from anecdote into data | med / low | **adopt (trial)** | economy / bugs-telemetry | `diag_log`+`str`+`+`; counter at `Server_AI_Com_Upgrade.sqf:134`; income interval only |
| 11 | **Fix the phantom capture counter** — drop the `CAPTURED` match (`wasp-b57-soak.ps1:51`), count real `CAPTURE` events | Historically false metric; B66 soak shows WASPSTAT-captures (3/6) already tracking — **likely already fixed; verify + close**. Harness-only | med / low | **adopt (verify-close)** | bugs-telemetry | harness-only |
| 12 | **TRIAL: shrink `restorer_buildings` scan to per-HQ bubbles** — replace the 10.5 km `nearestObjects` (`buildings_restorer.sqf:14`) with two ~1500 m HQ scans | Kills a map-wide candidate scan; cadence already floored at 1800 s — touch only the radius | med / low-med | **trial** | performance | TRIAL on test box; must prove BOTH base areas stay covered |
| 13 | **War-map timelapse (spectacle, BUILD THIRD)** — replay CAPTURE → PNG frames → GIF/MP4 off-box | Shareable, off-box, zero risk; shares #8/#9's parser | med / med-high | **trial** | spectacle | off-server; build after #8/#9 prove the pipeline |

**Below the top 13 (valid, lower priority):** antistack_main/flush ms + restorer ms in the soak harness (performance, adopt — signal only, the cost is off-limits); the external `asr_ai\sys_aiskill\fnc_fired.sqf:118` error (mod, not ours — note, can't fix); engine-flood ("Message not sent"/"Object not found") tripwire in the harness (bugs, adopt). **PARKED-as-insurance:** the B59 strategy freeze fixes (maneuver-vs-garrison gate, non-reset strand counter) — fronts are MOVING through B66, build only if the front re-freezes over a full round.

**REJECTED this cycle / standing rejects:** spawn-point relocation for vehicle teams (#1's recovery makes prevention unnecessary; map-specific, regression-prone — revisit only if a *specific* base spawn marker is the repeat offender); any income/balance tune (income is symmetric, the banking valve amplifies the winner — manufacturing asymmetry is the explicit DO-NOT-TOUCH); the old flag-then-HC-delete reclaim of healthy teams (superseded by #5's HC-aware disband + #6's add-only refill).

---

## By facet

### aicom-units & the stuck-team pathologies _(MOST-IMPLICATED — TWO bugs firing live)_
- **Pathology 1 — EAST `O 1-2-F` infinite retreat loop.** The live B66 retreat path (`Produce.sqf` retreat block) has **no termination**: any `alive<2 && dist>homeR` team is re-ordered `[seq+1,"DEFENSE",hqPos]` every 45 s PRODUCE cycle forever. `O 1-2-F` does this 30+ times, identical `dist=5566.83`, never closing. It reaches this branch **despite** the `wfbe_aicom_hc` gate (`:69`) while `srvTeams=0` → it's an HC team whose HC-flag reads false (re-adopted orphan). **Fix: #3 (tag, prove it) then #5 (HC-aware disband, hardened).**
- **Pathology 2 — WEST `B 1-2-F` vehicle spawn-wedge.** `distStart=0 moved=0`, 5 strikes, `elapsed=483`. Root cost = latency to the only tier that works (tiers 1-2 useless vs a base wedge). **Fix: #1 — `distStart≈0 → tier≥3` on the first strike.** Highest-confidence code win this cycle.
- **The uncommitted B67 cull (#5) is the standout HAZARD:** it's already drafted in the working tree and would server-side-delete HC-local units → orphan/frozen AI. Must be made HC-local-delete-only + COMBAT-guarded before it's safe.
- **The bleed (#6 HCTopUp) is the root-cause refill fix** — gated on #2 (classifier) + writing both missing halves. Add-only; never panic-delete healthy teams.

### economy & balance _(re-pressure-tested — verdict UNCHANGED: change nothing, measure)_
- **DO-NOT-TOUCH-BALANCE HOLDS, with one correction.** Income is `GetTownsSupply` per side (`updateresources.sqf:56`) × multipliers computed **once outside** the per-side loop (`:31-46`) — no per-side constant. The bootstrap floor + funds-fallback (`:94-122`) keep a 0-town side breathing; founding spends FUNDS (`AI_Commander_Teams.sqf:437-455`). **Correction to the old plan:** the banking valve (`Teams.sqf:57-100`) is `base + floor(funds/perExtraTeam)` — funds track towns, so it **amplifies the WINNER's lead**, it does NOT preferentially rescue the loser. The 2:1 bleed is a textbook emergent positive-feedback snowball (more towns → more income → more/fuller teams → more towns), NOT an income asymmetry.
- **The fix for the snowball is REFILL (#6), which lifts the floor on both sides equally — not income.** REJECT raising `SUPPLY_INCOME_MULT`, per-side handouts, supply-gated founding, and "wait-to-mass" staging (frozen-AI hazard). ADOPT only diagnostics (#10 SUPPLYSTAT; #2 corrects the inf/veh split that's currently always-wrong).

### aicom-strategy & front-stall _(front MOVING through B66 — freeze fixes remain insurance)_
- WEST PRESS / EAST DEFEND, real flips, two self-corrected SPEARHEAD_REPICK (Kabanino→Grishino→Petrovka). The B59 `contested=0` partition is NOT the failure mode; the strand short-circuit (#1) is the active strategy item because stuck teams sap the spearhead.
- The one urgent strategy item beyond #1 is **#4 (MHQ deploy-time enemy guard)** — see B60.
- POSTURE remains largely dead telemetry — do NOT bolt behaviour onto it.

### performance / FPS _(B66 holds; the sag is still the spawn burst)_
- B66 mid-game **33-47 at 4-5 towns**; HC-2 touched **17** once at the units=413 peak. **ADOPT #7 (stagger activations); TRIAL #12 (restorer per-HQ bubbles).**
- **REJECT (debated):** per-HC group caps (strand AI), body/wreck-timeout cuts (nothing accumulating), live group-merging, lowering `TOWNS_ACTIVE_MAX`. **antistack is the dominant cost but OFF-LIMITS by rule** — flatten the burst instead.

### player spectacle & AI cinematics _(verdict UNCHANGED: consume the firehose off-server)_
- Build order, all OFF-SERVER, zero live-risk: **#8 Front-Status embed FIRST → #9 LLM recap SECOND → #13 timelapse THIRD.**
- **REJECT all in-mission spectacle** (markers dominated by #8; director-cam collides with no-sim-gating + never-idle-AI + antistack).

### bugs & telemetry
- **Adopt the read-only fixes:** #2 (vehTeams classifier — real bug), #3 (retreat-tag + RETREATSTAT), #10 (SUPPLYSTAT), #11 (phantom counter verify-close), engine-flood tripwire.
- B66 live: 0 mission-code script errors; 1 external `asr_ai` error (mod). Benign net-despawn races only. Leave the engine alone.

---

## B60 / MHQ-RELOCATION — **CONDITIONAL-GO (stable across multiple live fires; one deploy-time ENEMY guard still open)**

- **MHQRELOC is now confirmed STABLE:** this cycle it ran full **TRIGGER (frontDist=2850) → MOBILIZED (LAV25_HQ) → DEPLOYED (reason=arrive)** twice, no stranding, no errors, plus the B65 first-fire. The feature works.
- **Plan #4 (deploy-time ENEMY re-check) is STILL GENUINELY OPEN.** The deploy step (`AI_Commander_MHQReloc.sqf:226-238`) computes `_finPos`, avoids water, ExecVMs `Construction_HQSite.sqf` with **no enemy test** after a ≤600 s drive — it survived only because the tile was clear. B66's guards are player/orphan-facing. **Insertion: immediately before the ExecVM at `:238`; `_enemyClear`/`_enemySide` are already Spawn params (`:106-107`).** Keep default-ON.
- **Heli cannon-nudge:** MOOT — B66 scoped it to heli-teams-only + fixed the muzzle. Do NOT re-propose disabling.
- **AICOM air-gate 4→3:** CONFIRMED + CLOSED (`Init_CommonConstants.sqf:166`).
- **REJECT** disabling MHQ-reloc (patchable) and decoupling the respawn anchor from `GetSideHQ` (the deploy guard mitigates the same risk far cheaper).

---

## Open questions for Ray

1. **Pathology 2 (spawn-wedge strand) is firing live** — green-light the **`distStart≈0 → tier≥3` short-circuit (#1)**? Reclaims ~420 s + 4 strikes per wedge, reuses the existing player-guarded teleport. **Recommend yes — highest-confidence win.**
2. **There is an UNCOMMITTED B67 retreat-cull in the working tree (`Produce.sqf`).** It is **unsafe as-is** (server-side `deleteVehicle` on an HC-local team → orphan/frozen AI; no COMBAT guard). **Recommend: do NOT commit it; rewrite as an HC-local disband-flag (#5) + COMBAT guard, gated on the #3 retreat-tag confirming the team is HC.** Agree?
3. **`vehTeams=0` is a real telemetry bug** (HC teams never get `wfbe_teamtype`). Green-light the classifier fix (#2)? It's read-only, and it **unblocks the HCTopUp armour guard** so #6 becomes safe.
4. **The bleed is a measured 2:1 snowball.** OK to **TRIAL the add-only HCTopUp refill (#6, default-OFF)** AFTER #2 lands and after writing its missing caller + consumer? **Recommend yes — it's the root-cause fix and it's balance-neutral.**
5. **MHQ-reloc deploy-time ENEMY re-check (#4)** still open after B66 (feature otherwise stable). Green-light the surgical guard at `:238`, keep default-ON?
6. **Economy: DO-NOT-TOUCH re-confirmed** (income symmetric; banking valve amplifies the winner). OK to change NOTHING in balance and just land the diagnostics (#2, #10)?
7. **Spectacle:** green-light building the off-server **Front-Status embed (#8)** first, then the LLM recap (#9)? All off-server, zero live-server risk.

---

## What NOT to touch

- **Do NOT commit the working-tree B67 retreat-cull unchanged.** Server-side delete of HC-local units orphans them → frozen AI. Use the HC-local disband-flag (#5) + COMBAT guard instead.
- **The live economy — change nothing.** Income is symmetric + town-supply-keyed; the banking valve amplifies the winner, so manufacturing a losing-side handout is the exact DO-NOT-TOUCH violation. The bleed is a refill problem (#6), not balance. Measure (#2, #10) first.
- **No "wait-to-mass" / hold-for-buddies AI staging.** One bug from a frozen AI — banned. Concentration comes from the spearhead picker.
- **Do NOT re-propose disabling the heli cannon-nudge** — B66 scoped it to heli-teams-only + fixed the muzzle; moot.
- **No live-server spectacle.** All adopted spectacle is OFF-SERVER (Discord embed/recap/timelapse from already-emitted telemetry).
- **antistack — hands-off entirely.** Only safe lever is the input (total AI / PC_LOW). Flatten the town-activation burst (#7) instead.
- **Performance traps to AVOID** (debated, rejected): per-HC group caps, body/wreck-timeout cuts, live group-merging, lowering `TOWNS_ACTIVE_MAX`.
- **No spawn-point relocation for vehicle teams** unless a specific base spawn marker is identified as the repeat offender (#1's recovery makes generic relocation unnecessary and regression-prone).
- **No sim-gating, no frozen AI.** Every code-shaped item re-tasks/teleports/refills/measures and re-wakes; none idles an AI.
- **Do not deploy. Do not merge this branch. Propose-only.** (Builds are deployed by Ray.)
</content>
</invoke>
