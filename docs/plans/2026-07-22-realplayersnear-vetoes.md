# RealPlayersNear Vetoes Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ensure recovery and cleanup vetoes count only living human players, never corpses or headless-client bodies.

**Architecture:** Add one common SQF function that counts living, non-HC players near a position, with an optional side filter. Replace the listed direct `playableUnits`/`allUnits` scans with calls to that function while retaining each scan's original radius and boolean behavior.

**Tech Stack:** Arma 2 OA 1.64 SQF; Python static-source regression tests; LoadoutManager terrain mirroring.

---

### Task 1: Specify the helper and call sites

**Files:**
- Create: `Tools/Lint/test_realplayersnear_vetoes.py`
- Test: `Tools/Lint/test_realplayersnear_vetoes.py`

**Step 1:** Add static assertions requiring the helper source, common registration, HC exclusions, and helper calls at each listed veto.

**Step 2:** Run `python Tools/Lint/test_realplayersnear_vetoes.py` and confirm it fails because the helper and registrations do not exist.

### Task 2: Implement and use the common helper

**Files:**
- Create: `Missions/[55-2hc]warfarev2_073v48co.chernarus/Common/Functions/Common_RealPlayersNear.sqf`
- Modify: `Missions/[55-2hc]warfarev2_073v48co.chernarus/Common/Init/Init_Common.sqf`
- Modify: listed Chernarus recovery, patrol, cleanup, USV, and commander SQF call sites.

**Step 1:** Implement an A2-OA-safe count function with `alive`, `isPlayer`, registered-HC, known-HC-name, CIVILIAN-seat, radius, and optional-side checks.

**Step 2:** Replace each direct player scan with the function, retaining its existing radius and veto polarity.

**Step 3:** Run `python Tools/Lint/test_realplayersnear_vetoes.py` and confirm it passes.

### Task 3: Mirror and validate

**Files:**
- Modify: Takistan/Zargabad mirrors generated by LoadoutManager.

**Step 1:** Run the specified SQF lint gate and bracket checks for edited files.

**Step 2:** Run LoadoutManager, restore terrain-specific version templates, and run `-- --check`.

**Step 3:** Commit and open a draft PR stacked on #1277, declaring the unrelated #1269 file overlap.
