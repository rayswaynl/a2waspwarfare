# 4-HC soak ideation tick — 2026-07-26 13:xx Europe/Amsterdam

Provider: `codex` (rotation rung for this tick).

Scope: read-only SSH sweep of the live 4-HC soak box, each RPT windowed from its latest `MISSINIT`:

- Server: `C:\Users\Administrator\AppData\Local\ArmA 2 OA\arma2oaserver.RPT`
- HC1: `C:\Users\Administrator\AppData\Local\ArmA 2 OA\ArmA2OA.RPT`
- HC2–HC4: `C:\Sandbox\Administrator\HC{2,3,4}\user\current\AppData\Local\ArmA 2 OA\ArmA2OA.RPT`

No process, mission, deployment, or box file was changed.

## Match read

The current Chernarus window is active rather than a dead boot: server telemetry contains seven `WASPSCALE`, 333 `WASPSTAT`, 1,028 `AICOMSTAT`, 389 `AICOM2`, seven `GRPBUDGET`, and seven `SRVPERF` records. The latest strategic snapshots show EAST at 9 towns / 8 teams in `DEFEND`, and WEST at 7 towns / 30 teams in `PRESS`; WEST also has active recovery, dispatch, arrival, and forward-base events. GUER director ledger records remain present (`towns=32`, nonzero strength and regeneration debt), with recent WEST↔GUER and EAST↔GUER kills. This is a contested, operating match, not evidence of a GUER collapse or an unopposed steamroll from this single window.

## Issue triage

The only recurring mission error family that needs attention is already owned by the active merged-batch soak verification: `Common_RunCommanderTeam.sqf:3000` reports `_topNear` undefined immediately after `WFBE_CO_FNC_RealPlayersNear` during top-up spawning. The fresh direct counts were HC1=1, HC2=4, HC3=4, HC4=3. Every sampled traceback points to the same caller and occurs amid normal unit-creation / top-up activity.

This is **not filed as a new issue**. Current `origin/master` already contains the source-side seed/fallback correction in commit `55d2b21f3b0565f777399518f2c4a0fc87e4dfd4`, and open draft PR [#1480](https://github.com/rayswaynl/a2waspwarfare/pull/1480) is independently verifying the exact stale-runtime family. The live log therefore cannot prove the fix failed: its current `MISSINIT` predates a fresh box build carrying that commit.

There were no `Script error` or `No entry` records in the five current windows. Other `Error in expression` volume must stay separated by signature: HC4 has a larger unrelated air-idle family already tracked by `wasp-idlertb-undefined-hc-20260726`; it is not attributed to the `_topNear` fix.

## Grounded next idea / gate

Do not add a new gameplay feature from this tick. The highest-value next observation is a clean-MISSINIT confirmation after the next owner-controlled deployment: window all four HC RPTs from that new boundary and require `_topNear` / `WFBE_CO_FNC_RealPlayersNear` error counts to remain zero while TOPUP activity still occurs. That preserves the useful player-proximity safety behavior while proving its failure path no longer aborts the top-up consumer.

## Dedupe and handoff

Checked current open PRs before writing. No mission code was edited and no new fix PR was created. This tick is deliberately a documentation-only handoff to #1480 and the next differently-provided hourly observer.
