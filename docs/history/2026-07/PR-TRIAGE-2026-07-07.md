# PR Triage — 2026-07-07

<!-- GUIDE-REV GR-2026-07-07a — docs only, no runtime/code changes in this file. -->

Classification of the 6 currently open PRs in the `control-bloat` survey cluster: a
5-PR stacked soak-tooling chain (#880 -> #882 -> #883 -> #884 -> #885) plus one
standalone mission-code PR (#886). All are DRAFT, based on/targeting
`claude/build84-cmdcon36`.

## Summary

| PR | Title | Base | Mergeable | CI | Classification |
|---|---|---|---|---|---|
| #880 | feat(soak): soak-ledger data spine | claude/build84-cmdcon36 | MERGEABLE | UNSTABLE (pre-existing, unrelated) | MERGE-CANDIDATE |
| #882 | feat(soak): scenario catalog + Run-Scenario driver | fable/soak-ledger (stacked on #880) | MERGEABLE | CLEAN (no checks yet) | MERGE-CANDIDATE (after #880) |
| #883 | feat(soak): dependency-free SVG chart report | fable/scenario-driver (stacked on #882) | MERGEABLE | CLEAN | MERGE-CANDIDATE (after #882) |
| #884 | feat(soak): experiment engine core | fable/soak-charts (stacked on #883) | MERGEABLE | CLEAN | MERGE-CANDIDATE, code-safe — but MDE thresholds need owner sign-off before findings are trusted |
| #885 | feat(soak): autopilot loop | fable/autopilot-engine (stacked on #884) | MERGEABLE | CLEAN | MERGE-CANDIDATE (after #884) |
| #886 | feat(ctl): Commander Town Ledger | claude/build84-cmdcon36 | MERGEABLE | PENDING | MERGE-CANDIDATE |

## Stacked chain: #880 -> #882 -> #883 -> #884 -> #885 (soak tooling)

The 5-PR chain forms one coherent stack — merge in order, top of stack (#885) last.

### #880 — feat(soak): soak-ledger data spine
- **Diff**: +871 / -0 across 7 files, all under `Tools/Soak`.
- **Content**: append-only ledger + JSON-schema contract; PR body claims 24/24 tests
  green.
- **CI**: sole failing check is "Tooling and report-only audits", which fails on a
  pre-existing AGENTS.md vs CLAUDE.md doc-sync drift (line 130, `DBLBOM`/`TRAILCOMMA`
  codes present in CLAUDE.md but not AGENTS.md) — this is a **base-branch state issue
  that predates this PR and blocks all 6 PRs equally**, not a diff-introduced
  regression. Recommend a separate 1-line doc-sync fix PR so it stops gating the whole
  stack.
- **Verdict**: MERGE-CANDIDATE. Foundation of the stack; no mission/flag surface.

### #882 — feat(soak): scenario catalog + Run-Scenario driver
- **Diff**: +691 / -0 across 7 files. Stacked on #880.
- **Content**: 7 named recipes including `hc-split` and `a-life-probe` (GUER Director
  aliveness), matching current research interests. Spawn-free by design (no Arma
  launch).
- **CI**: 0 checks configured yet — expected to inherit #880's CI once rebased onto
  `build84-cmdcon36` post-merge.
- **Verdict**: MERGE-CANDIDATE once #880 lands.

### #883 — feat(soak): dependency-free SVG chart report
- **Diff**: +509 / -2 across 4 files. Stacked on #882.
- **Content**: hand-rolled SVG (no matplotlib/numpy dependency), theme-aware, treats
  null as distinct from zero. Self-test claimed 10/10 in PR body.
- **Verdict**: MERGE-CANDIDATE once #882 lands.

### #884 — feat(soak): experiment engine core
- **Diff**: +819 / -6 across 7 files. Stacked on #883.
- **Content**: `mde-table.json` per-metric minimum-detectable-effect + direction table
  that will gate PASS/FAIL significance verdicts.
- **Owner gate**: the PR body explicitly self-describes the MDE thresholds as
  "proposed; owner sign-off pending." The code itself is safe to merge (no mission/flag
  surface, doesn't mutate anything by itself) — but any experiment result produced by
  this engine should be treated as provisional until the thresholds get a nod. See
  `OWNER-QUESTIONS-BEFORE-BUILD.md` Q-BLOAT-1.
- **Verdict**: MERGE-CANDIDATE for the code; thresholds are a distinct owner-decision
  line item, not a merge blocker.

### #885 — feat(soak): autopilot loop
- **Diff**: +778 / -6 across 10 files. Stacked on #884. Top of the 5-PR chain
  (880 -> 882 -> 883 -> 884 -> 885).
- **Content**: `Get-FlagRecommendation.ps1` is explicitly surface-only per owner ruling
  — disposition always SURFACE, `autoApplied` always false, no PR/deploy code path.
  Matches the V2 no-unattended-mutation constraint. Never spawns Arma.
- **Verdict**: MERGE-CANDIDATE once #884 lands. Safe by construction (recommend-only,
  no auto-apply).

## Standalone: #886

### #886 — feat(ctl): Commander Town Ledger
- **Base**: `claude/build84-cmdcon36` (not stacked on the soak chain).
- **Diff**: +2,343 / -1 across 22 files — the largest of the 6 open PRs, and the only
  one touching mission SQF directly.
- **Content**: implements the already-approved CTL spec (PR #807, merged 2026-07-07
  07:20 UTC, docs-only). Checked `gh pr list --state merged --search 'town ledger OR
  CTL'` — no other CTL implementation PR found, so this is not a duplicate/superseded
  lane.
- **Flag policy**: all 16 flags are append-only in `Init_CommonConstants.sqf:2438-2453`.
  Master switch `AICOMV2_LANE_CMD_TOWN_LEDGER` defaults 0; sub-flag
  `AICOMV2_CTL_INVEST_ENABLE` (AI spends funds to raise town strength) also defaults 0.
  Flag-off claimed byte-identical to HEAD.
- **Scope note**: explicitly distinguishes itself from the independent GUER
  garrison-gain workstream (#819/#835/#848/#877) — GUER is untouched by this PR.
- **CI**: sole check "Tooling and report-only audits" currently PENDING (not failed).
- **Verdict**: MERGE-CANDIDATE. Once merged, flipping `AICOMV2_CTL_INVEST_ENABLE` is a
  distinct owner-timing decision (see `OWNER-QUESTIONS-BEFORE-BUILD.md` Q-RELOC-1(a)),
  not a merge blocker for the PR itself.

## Recommended merge order

1. #880 (unblock the base-branch doc-sync CI issue separately, in parallel — do not let
   it gate this stack)
2. #882
3. #883
4. #884 (code merges now; MDE thresholds get owner review before results are trusted)
5. #885
6. #886 (independent of the above, can merge in any order relative to the soak chain)
