# 4-HC soak ideation tick — 2026-07-27 06:48 Europe/Amsterdam

Provider: `codex` (assigned lane for this tick).

Scope: a read-only SSH sweep of the live Chernarus server and all four HC RPTs, each windowed from its latest `MISSINIT` through EOF:

- Server: `C:\Users\Administrator\AppData\Local\ArmA 2 OA\arma2oaserver.RPT`
- HC1: `C:\Users\Administrator\AppData\Local\ArmA 2 OA\ArmA2OA.RPT`
- HC2–HC4: `C:\Sandbox\Administrator\HC{2,3,4}\user\current\AppData\Local\ArmA 2 OA\ArmA2OA.RPT`

No process, mission, deployment, or box file was changed.

## Match read

The Chernarus mission is live and exercising all four HCs. The server window contains nine `WASPSCALE`, 361 `WASPSTAT`, 1,295 `AICOMSTAT`, 496 `AICOM2`, nine `GRPBUDGET`, and nine `SRVPERF` records. Its latest samples show GUER air-defence creation at Petrovka and a director ledger with 46 towns, nonzero strength, funding, and regeneration debt.

All HC windows contain active AICOM and GUER work: HC1/HC2/HC3/HC4 logged 551/315/184/457 `AICOMSTAT` records and 102/87/109/130 GUER records respectively. HC2–HC4 each also logged one `TOPUP` event. HC1, HC3, and HC4 recorded Petrovka GUER town activation/creation activity, while HC2 recorded an active capture transition. This is an operating, multi-HC match window rather than an idle boot.

## Issue triage

All five latest-`MISSINIT` windows have zero matches for `Script error`, `Error in expression`, `Undefined variable`, `No entry`, `_topNear`, and `RealPlayersNear`. The exercised HC2–HC4 TOPUP activity makes the clean `_topNear` count useful, but it is an observation rather than proof of a package/deployment transition.

No new issue is filed. The source-side guard is present in current `origin/master` at commit `55d2b21f3b0565f777399518f2c4a0fc87e4dfd4` (`fix(aicom): exclude HC4 from RealPlayersNear vetoes + stop the _topDefer cascade`). The antecedent failure analysis remains owned by open PR [#1480](https://github.com/rayswaynl/a2waspwarfare/pull/1480); open PR [#1487](https://github.com/rayswaynl/a2waspwarfare/pull/1487) separately records the earlier dbg0726g five-RPT audit. Neither is duplicated here.

## Grounded next idea / gate

Do not add a gameplay feature from this clean tick. The highest-value next observation is a second clean latest-`MISSINIT` sweep after additional TOPUP activity: require all five RPTs to retain zero `_topNear` and `RealPlayersNear` errors while the server still emits `WASPSCALE`/`AICOMSTAT` and at least one HC records TOPUP. That distinguishes a quiet interval from a sustained healthy consumer path without weakening player-proximity protection.

## Dedupe and handoff

Checked open soak PRs before writing. This report continues rolling ideation PR [#1482](https://github.com/rayswaynl/a2waspwarfare/pull/1482) as a stacked documentation-only PR; no mission code or additional fix PR was created. GitHub Issues are disabled for this repository, so PR/task-card dedupe is the available issue-equivalent check.
