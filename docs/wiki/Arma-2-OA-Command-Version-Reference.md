# Arma 2 OA — SQF command version reference (A3 contrast)

> Independent BIKI-verified **version-evidence companion** to the canonical [External Arma 2 OA reference guide](Arma-2-OA-External-Reference-Guide). That page maps concepts → references and how-to-use; **this page is the dated provenance log**: the *first game / version* of each command this fork touches, so an agent can decide at a glance whether a command is safe for the **Arma 2 OA 1.63 / 1.64** target. Verified read-only against the Bohemia Community Wiki (`community.bistudio.com`) on 2026-06-02 — reading a command's version badge needs no account.

## Why this matters
The fastest way to break OA mission SQF is to "fix" it with Arma 3 reflexes: many everyday A3 commands simply did not exist in 2010–2013 and throw *undefined command* errors on OA. This page is the quick yes/no, with the OA-safe replacement.

## Arma 3-only — do NOT use in OA mission SQF

| Command / form | First game · version | Arma 2 OA replacement |
| --- | --- | --- |
| `params ["_a","_b"]` | Arma 3 · 1.48 | `private ["_a","_b"]; _a = _this select 0; _b = _this select 1;` |
| `setGroupOwner` / `groupOwner` | Arma 3 · 1.40 | **No equivalent** — OA cannot transfer group locality; it follows the group leader's owner. (Impacts DR-21 HC re-delegation: you can redirect *future* spawns to a surviving HC, but cannot transfer live AI.) |
| `array select [start, count]` | Arma 3 · 1.32 | single-index `select N`, or build with `for`/`forEach`. |
| `string select [start, length]` (substring) | Arma 3 · 1.28 | `toArray` / `toString` (no native substring in OA). |
| `array select {expression}` (filter) | Arma 3 · 1.56 | `{ if (cond) then { … } } forEach _arr`. |
| `array apply {expression}` (map) | Arma 3 · 1.56 | Build a new array explicitly in a `for`/`forEach` loop, e.g. `_out = []; { _out set [count _out, (_x call _fnc)] } forEach _arr;`. Source has **zero** `apply` array-command uses (only the English word in comments); it previously appeared in a draft doc fix-snippet and is kept here as a regression warning. |
| `isEqualType` | Arma 3 · 1.54 | `typeName _x == "ARRAY"` (etc.). |
| `remoteExec` / `remoteExecCall` | Arma 3 · 1.50 | `publicVariable` + `addPublicVariableEventHandler` (this mission's PVF wrappers). |
| `compileFinal` | Arma 3 · 0.56 | `compile`; keep server authority in validated handlers, not in A3 read-only code objects. Source has **zero** live `compileFinal` hits. |
| `canSuspend` | Arma 3 · 1.58 | No OA equivalent. Decide scheduling from callsite ownership: `spawn`/`execVM` code may suspend; direct `call`/event-handler code must be audited before adding `sleep`/`waitUntil`. Source has **zero** live `canSuspend` hits. |
| `waitUntil [condition, timeout, interval]` / `waitUntil scriptHandle` | Arma 3 · 2.22 | OA supports `waitUntil { condition }` only. For timeouts/intervals, write the loop explicitly with `diag_tickTime` + `sleep`/`uiSleep`; for handles use `waitUntil {scriptDone _handle}`. |
| `parseSimpleArray` | Arma 3 · 1.68 | `call compile` — trusted input only (see DR-1 / DR-7 / DR-46). |
| `ext callExtension [fn, args]` (array form) | Arma 3 · 1.68 | string form: `ext callExtension "code,args"`. |
| `private _x = value` (inline-assign) | Arma 3 style | `private "_x"; _x = value;` |
| `setUnitLoadout` / `getUnitLoadout` | Arma 3 · 1.58 | **No OA equivalent.** OA loadouts come from config classes plus `addWeapon`/`addMagazine`/`*CargoGlobal` and `setVehicleInit`. **Confirmed absent from source (0 hits):** `Tools/LoadoutManager` generates config-driven loadouts; it does not use the A3 loadout API. Do not introduce. |
| `hideObjectGlobal` / `enableSimulationGlobal` | Arma 3 · 1.12 | A3-only **Global** variants. OA only has the **local** `hideObject` / `enableSimulation`; the effect is per-machine and must be re-applied (or driven by the mission's PV pattern) for global state. **Confirmed absent from source (0 hits)** — do not introduce a Global variant by reflex. |
| `createVehicleCrew` | Arma 3 · 0.76 | Create crew explicitly with OA `createUnit` / `moveIn*` / existing Wasp factories; do not use the A3 auto-crew helper. |
| `deleteVehicleCrew` | Arma 3 · 1.32 | Delete/eject crew with OA-compatible source patterns before deleting the vehicle; do not paste the A3 helper into OA SQF. |
| `allPlayers` | Arma 3 · 1.48 | Use `playableUnits` plus `isPlayer` filtering where needed. `playableUnits` is MP-only and does not include dead players awaiting respawn, so source-specific cleanup/score logic still needs the right unit/crew/UID checks. |
| `getMissionConfigValue` | Arma 3 · 1.56 | Use `getNumber` / `getText` / `getArray` against `missionConfigFile >> ...`, with explicit defaults where the source path needs one. |
| `configClasses` | Arma 3 · 1.24 | Iterate OA config children with `for "_i" from 0 to count _config - 1 do { _child = _config select _i; if (isClass _child) then { ... } };`. |
| `configProperties` | Arma 3 · 1.36 | Same manual `count` / `select` / `isClass` or `isNumber` / `isText` / `isArray` scan, depending on the expected entry type. |
| Arma 3 mission parameter templates (`#include "\a3\..."`) | Arma 3 framework | Define this mission's `class Params` in `Rsc/Parameters.hpp`; keep `paramsArray` index order stable. |
| `selectRandom` | Arma 3 · 1.56 | Use the repo idiom `_arr select floor(random count _arr)` (`Common/Init/Init_Town.sqf:39`, `Server/AI/AI_AdvancedRespawn.sqf`) **or** `_arr call BIS_fnc_selectRandom` (OA-safe, see below). **Confirmed absent** — the 4 `selectRandom` source hits are all `BIS_fnc_selectRandom`. ⚠ Do not "simplify" `call BIS_fnc_selectRandom` into the `selectRandom` *command*: the function is OA-safe, the command is not. |
| `splitString` / `joinString` | Arma 3 · 1.50 | No OA native split/join — parse with `toArray`/`toString` + a manual `forEach`. **Confirmed absent (0 hits).** |
| `trim` (+ `regexFind` / `regexMatch`) | Arma 3 · 2.02 (regex 2.x) | No OA equivalent; trim/match manually via `toArray`/`toString`. **Confirmed absent.** |
| `isRemoteExecuted` / `remoteExecutedOwner` | Arma 3 · 1.66 | No OA remote-execution context exists. Wasp handlers must derive authority from server-owned mission state, the target object, UID/requester fields already in the payload, or an explicit OA-safe request/response design. |

## Confirmed available in Arma 2 OA

| Command / form | First · version | Note |
| --- | --- | --- |
| `obj getVariable ["name", default]` | ArmA 1.00 (obj/group); **OA 1.60** (namespace) | Default-value form is OA-safe on `missionNamespace`. CAVEAT: on `objNull`, A2 returns *undefined*, not the default (A3 fixed this). |
| `obj setVariable ["name", val, true]` | ArmA 1.00 | Public-broadcast 3rd arg is OA-safe. |
| `publicVariableServer` / `publicVariableClient` | **OA 1.62** | client→server / server→one-client targeting. |
| `callExtension` (string form) | **OA 1.60** | Blocking — keep out of hot loops. |
| `diag_tickTime` | **Arma 2 · 1.00** | Real-time, high-precision elapsed seconds; **unscaled** and does not pause with game time. Commonly mis-remembered as A3-only, but it is A2-era and OA-safe — the correct clock for perf instrumentation. Repo uses it as the `PerformanceAudit_Record` stopwatch (~62 files, e.g. `Client/Client_UpdateRHUD.sqf:187`). Do **not** "modernize" it away. |
| `uiSleep` | **Arma 2 · 1.05** (also OA 1.50) | Like `sleep` but **not** scaled by accelerated/skipped game time — suspends on real-time cadence (correct for server housekeeping loops). Also frequently assumed A3-only; it is OA-safe. Repo uses it in the AntiStack loops (`Server/Module/AntiStack/mainLoop.sqf:16`, `flushLoop.sqf`) and `Server/FSM/restorers/buildings_restorer.sqf:26` (7 files). |
| `getPosATL` / `setPosATL` | **Arma 2 · 1.03** | Above-terrain-level coordinates. OA-safe (7 / 2 files). |
| `createVehicleLocal` | ArmA 1.00 | Creates a **client-local** object **not** synchronized over the network (`netId` is `0:0`) — use only for local FX/markers; other machines will not see it. 6 files. |
| `addWeaponCargoGlobal` / `addMagazineCargoGlobal` | **OA 1.55** | **Global** MP effect (cargo synced to all clients) — the MP-correct cargo add, **not** A3-only. Repo gear-equip path (`Common/Functions/Common_EquipBackpack.sqf`, `Common_EquipVehicle.sqf`). |
| `BIS_fnc_selectRandom` | **Arma 2 · 1.00** (OA 1.50) | OA-safe **function** (`_arr call BIS_fnc_selectRandom`) returning a random array element — **distinct from the A3-only `selectRandom` command** in the avoid-list above. Repo: spawn-pad pick in `Client/Functions/Client_BuildUnit.sqf:59/85/111/135`. |
| `allGroups`, `call`, `compile`, `preprocessFileLineNumbers`, `typeName`, `isNil`, `format`, `localize`, `hintSilent`, `diag_log`, `diag_fps`, `publicVariable`, `addPublicVariableEventHandler`, `toArray`, `toString`, `setVectorDirAndUp` (ArmA 1.09) | OFP / ArmA / A2 | All OA-safe. |

## Config and mission-parameter commands - OA-safe, order-sensitive

These commands are the fork's data-loading vocabulary for `description.ext` mission parameters, gear metadata, squad templates, backpacks, EASA missile classification and UI display names. The trap is replacing explicit OA config walking with newer Arma 3 helpers or treating `class Params` as SQF `params`.

| Command family | OA status / BI note | Wasp source anchor | Developer guardrail |
| --- | --- | --- | --- |
| `paramsArray` + `missionConfigFile` | `paramsArray` / mission parameters are Arma 2-era; BI's Mission Parameters page says secondary `class Params` values are stored in `paramsArray` in config order. `missionConfigFile` is Armed Assault 1.00 and OA-safe. | `description.ext:63` includes `Rsc\Parameters.hpp`; `Rsc/Parameters.hpp:3,5-9` defines `class Params` and the first parameter. Boot parameters iterate `missionConfigFile >> "Params"` and assign each `configName` into `missionNamespace`: `Common/Init/Init_Parameters.sqf:5-9`. The in-game parameter display launches from `Rsc/Dialogs.hpp:3136`, mirrors the same order and falls back to `default` in SP: `Client/GUI/GUI_Display_Parameters.sqf:3-12`. DR-35 already records this as reviewed-clean and wired from `initJIPCompatible.sqf:132`. | Keep `class Params` order stable. `paramsArray select _i` is index-aligned to `missionConfigFile >> "Params" select _i`, so inserting/removing/reordering parameters can silently shift every later value. This is mission config `class Params`, not A3 SQF `params`. |
| `configFile` + `getNumber` / `getText` / `getArray` | Armed Assault 1.00 config readers and OA-safe. Use the typed reader that matches the entry, and guard optional entries before reading if absence has gameplay meaning. | Gear/cargo UI reads CfgVehicles capacities and pictures (`Client/GUI/GUI_BuyGearMenu.sqf:81,382-393`); buy-unit UI reads transport/maxSpeed/armor/weapons/magazines (`Client/GUI/GUI_Menu_BuyUnits.sqf:278-280,401-402`); unit gear extraction reads weapons, magazines and backpack (`Common/Functions/Common_GetUnitConfigGear.sqf:11-14`). Weapon config loading validates `CfgWeapons` and reads typed metadata at `Common/Config/Config_Weapons.sqf:13,20,32,34-35`. IRS and missile helpers read CfgAmmo/CfgMagazines lock fields (`Client/Init/Init_Client.sqf:828-837`, `Common/Functions/Common_HandleShootMissiles.sqf:56-66`). | Do not replace direct config reads with A3 `getMissionConfigValue` or other modern helpers. If a config value is optional, follow existing source shape: `isClass`, `isNumber`, `isText` or `isArray` first, then read with the matching getter. |
| `isClass` / `configName` / manual `count` + `select` config walking | OA-safe config traversal. BI pages for `isClass`, `configName`, `count` and `select` support the old config-child iteration model; A3 `configClasses` and `configProperties` are not OA commands. | Squad template discovery validates and walks CfgGroups children manually: `Common/Config/Core_Squads/Squads_GetFactionGroups.sqf:8-10,23-31,41-43,60`; group lookup does the same in `Common/Functions/Common_GetGroupFromConfig.sqf:7-20`. Backpack parsing uses `isClass` and `count(configFile >> ... ) - 1` loops for nested transport classes: `Common/Config/Config_Backpack.sqf:12,32-56,70-76`. | Keep the manual scan when patching OA. Do not copy BIKI examples that use `configClasses`, `configProperties` or `apply`; use `for` + `count` + `select`, and filter each child with `isClass` or a typed entry check. |
| `inheritsFrom` + `configName` | Armed Assault 1.00 / OA-safe parent inspection. It is useful for engine class inheritance, but it returns a parent config, not a full ancestry list. | Shared config helper falls back to a parent when an entry is missing: `Common/Functions/Common_GetConfigEntry.sqf:20-28`. AA missile filtering checks ammo parent class names in `Common/Functions/Common_RemoveAAMissiles.sqf:22-28`; EASA uses the same pattern while tagging loadouts at `Client/Module/EASA/EASA_Init.sqf:667`. | Use this for the specific parent checks the source already performs. Do not substitute A3 hierarchy helpers or assume one `inheritsFrom` call proves every ancestor. If a future feature needs full ancestry, write an OA loop and validate on the target config classes. |
| `isNumber` / `isText` / `isArray` | Armed Assault 1.00 typed config-entry tests, OA-safe. | `Common/Functions/Common_GetConfigEntry.sqf:24-28` uses typed tests to select `getNumber`, `getText` or `getArray`. IRS handles optional config values with `isNumber` before defaulting in `Common/Module/IRS/IRS_Init.sqf:52` and `Common/Module/IRS/IRS_HandleMissile.sqf:31`. CoIn resolves icon/picture config entries only when they are text: `Client/Module/CoIn/coin_interface.sqf:761-764`. | Prefer typed checks over assuming an entry exists or has one shape across mods. This keeps modded config variance from turning into nil/undefined behavior in UI, gear or missile paths. |

A3 contrast: `getMissionConfigValue`, `configClasses`, `configProperties`, A3 `apply` examples and `#include "\a3\..."` parameter templates are not OA proof. For this mission, use `missionConfigFile` / `configFile`, `>>`, typed getters, `isClass`/typed guards and manual `count`/`select` loops.

Official references checked for this section: [Mission Parameters / paramsArray](https://community.bohemia.net/wiki/paramsArray), [missionConfigFile](https://community.bohemia.net/wiki/missionConfigFile), [configFile](https://community.bohemia.net/wiki/configFile), [getNumber](https://community.bohemia.net/wiki/getNumber), [getText](https://community.bohemia.net/wiki/getText), [getArray](https://community.bohemia.net/wiki/getArray), [isClass](https://community.bohemia.net/wiki/isClass), [configName](https://community.bohemia.net/wiki/configName), [inheritsFrom](https://community.bohemia.net/wiki/inheritsFrom), [isNumber](https://community.bohemia.net/wiki/isNumber), [isText](https://community.bohemia.net/wiki/isText) and [isArray](https://community.bohemia.net/wiki/isArray). A3-only contrast pages: [getMissionConfigValue](https://community.bohemia.net/wiki/getMissionConfigValue), [configClasses](https://community.bohemia.net/wiki/configClasses), [configProperties](https://community.bohemia.net/wiki/configProperties).

## Role, locality and targeted network commands - OA-safe, role-sensitive

These commands are the fork's server/client/hosted/HC vocabulary. The trap is treating one role test as another, or importing Arma 3 remote-execution identity helpers that OA does not have. For architecture detail, route to [Lifecycle wait-chain](Lifecycle-Wait-Chain), [Networking and public variables](Networking-And-Public-Variables), [Public variable channel index](Public-Variable-Channel-Index), [AI, headless and performance](AI-Headless-And-Performance) and the [Arma 2 OA compatibility audit](Arma-2-OA-Compatibility-Audit).

| Command family | OA status / BI note | Wasp source anchor | Developer guardrail |
| --- | --- | --- | --- |
| `isServer` / `isDedicated` | OA-safe role tests. BI documents `isServer` as true on dedicated servers, hosted/player servers and single player; `isDedicated` is true only on a dedicated multiplayer server. | Mission role split starts with `isHostedServer = (!isMultiplayer || (isServer && !isDedicated))` at `initJIPCompatible.sqf:63`. Server init runs for hosted or dedicated at `:239`; client init runs for hosted or non-dedicated non-HC clients at `:245`. `Server/Init/Init_Server.sqf:1` guards server init with `isServer`; dedicated-only server FPS publishers are currently loop-first and tracked in `Hosted-Server-FPS-Loop-Sleep`. | Do not collapse hosted and dedicated behavior. Hosted servers are both server and player client, so replacing `isHostedServer || isDedicated` with `isDedicated` can break hosted play; using `!isDedicated` includes headless clients unless another guard excludes them. |
| `hasInterface` / HC detection | `hasInterface` is OA 1.63+, not A3-only. BI documents it as true on machines with a player UI and false on dedicated servers and headless clients. | HC detector is exactly the BI shape: `Headless/Functions/HC_IsHeadlessClient.sqf:5` returns `!(hasInterface || isDedicated)`. The mission disables HC delegation only on old OA versions at `initJIPCompatible.sqf:165-171`, and the HC announces itself from `Headless/Init/Init_HC.sqf:15`. | Keep `hasInterface` in OA 1.64 code; do not "modernize" it away. For HC-only code use `!hasInterface && !isDedicated` or the existing `isHeadLessClient` helper, not plain `!isDedicated`. |
| `local` | OA-safe object-locality test. BI documents `local object`; group-argument examples and the `local variable = value` form on the page are version/syntax traps, so use Wasp's object checks for OA mission code. | Client player-AI recovery deliberately checks object locality before acting: `Client_WatchdogPlayerAI.sqf:199-200`, `Client_RecoverPlayerAI.sqf:389-398,828-829,873-874`. Client PVEH registration also keeps the hosted/player branch with `!isServer || local player` at `Common/Init/Init_PublicVariables.sqf:45`. Local vehicle-only fixes appear in Valhalla low gear (`Client/Module/Valhalla/Common_AI_LowGear.sqf:43-44`) and artillery refresh (`Client/Functions/Client_FNC_Special.sqf:184-193`). | Run movement/vehicle/AI state changes where the object is local. Use `local player` only as this mission's client/present-player gate, not as server authority. Do not copy A3 `local group` examples unless OA proof exists for the target syntax. |
| `owner` + `publicVariableClient` | `owner object` is OA-safe on the server and returns the client network id for the machine where the object is local; `publicVariableClient` is OA 1.62 targeted server-to-client sync. | `Common/Functions/Common_SendToClient.sqf:11-20` derives `_id = owner (_pvf select 0)`, rewrites the target player object to UID, and sends `WFBE_PVF_<Command>` to that one client. Direct unicast examples also include `Server/Functions/Server_PV_RequestSupplyValue.sqf:8` and `Server/Module/AntiStack/clientHasConnectedAtLaunch.sqf:15`. HC registration checks `owner _hc` before storing a candidate: `Server/Functions/Server_HandleSpecial.sqf:120-130`. | `owner` is a routing primitive, not proof of gameplay authority. If `owner _obj` is `0`, Wasp's HC registration currently skips the HC; future HC fixes should retry/log rather than assume a valid client id. Prefer `SendToClient`/`publicVariableClient` over broadcast+UID filtering when true unicast is intended. |
| `publicVariableServer` / `addPublicVariableEventHandler` | `publicVariableServer` and `publicVariableClient` are OA 1.62 tools; PVEH observes received variable name/value. BI's current pages include A3 notes, but OA Wasp handlers do not get a trusted hidden sender identity. | Optimized PVF client-to-server send is `Common/Functions/Common_SendToServerOptimized.sqf:15`; direct channels include attack waves at `Common/Functions/Common_AttackWaveActivate.sqf:8`, side supply at `Common/Functions/Common_ChangeSideSupply.sqf:30`, and client boot ACKs in `Client/Init/Init_Client.sqf:443,454,761,769,963`. PVF PVEHs are role-gated in `Common/Init/Init_PublicVariables.sqf:45,50`. | Keep direct-PV hardening separate from PVF dispatcher hardening. A PVEH means a payload arrived, not that the sender had commander role, range, funds or side authority. Preserve hosted local-dispatch branches in the send helpers. |
| `isPlayer` / `playableUnits` | OA-safe player/playable-unit queries. BI documents `playableUnits` as multiplayer playable units and warns it is empty in single player; modern `allPlayers` examples are A3-only. `isPlayer` can be true for HCs, so use role context. | Disconnect cleanup avoids deleting playable slots: `Server/Functions/Server_OnPlayerDisconnected.sqf:109`. AI respawn guards server work with `isServer && !(isPlayer ...)` in `Server/AI/AI_AddMultiplayerRespawnEH.sqf:1`. DR-45's town-AI vehicle deletion risk comes from testing only `isPlayer leader group _x` before deletion at `Server/FSM/server_town_ai.sqf:214`. | `isPlayer leader group` is not a full player-occupancy test. Before deleting vehicles or awarding/withholding player effects, check the actual unit/crew/cargo cases the feature cares about. Do not replace `playableUnits` with A3 `allPlayers` in OA snippets. |

A3 contrast: `setGroupOwner` / `groupOwner`, `remoteExec` / `remoteExecCall`, `remoteExecutedOwner`, `isRemoteExecuted`, `allPlayers` and A3 group-locality examples are not OA proof for this mission. For Wasp, use source-backed publicVariable/PVF routing, object `local` checks and future-spawn HC routing rather than live group transfer.

Official references checked for this section: [isServer](https://community.bohemia.net/wiki/isServer), [isDedicated](https://community.bohemia.net/wiki/isDedicated), [hasInterface](https://community.bohemia.net/wiki/hasInterface), [local](https://community.bohemia.net/wiki/local), [owner](https://community.bohemia.net/wiki/owner), [publicVariableClient](https://community.bohemia.net/wiki/publicVariableClient), [publicVariableServer](https://community.bohemia.net/wiki/publicVariableServer), [addPublicVariableEventHandler](https://community.bohemia.net/wiki/addPublicVariableEventHandler), [isPlayer](https://community.bohemia.net/wiki/isPlayer) and [playableUnits](https://community.bohemia.net/wiki/playableUnits). A3-only contrast pages: [isRemoteExecuted](https://community.bohemia.net/wiki/isRemoteExecuted), [remoteExecutedOwner](https://community.bohemia.net/wiki/remoteExecutedOwner), [setGroupOwner](https://community.bohemia.net/wiki/setGroupOwner), [groupOwner](https://community.bohemia.net/wiki/groupOwner).

Validation handoff: this pass is source-only and official-docs checked. Runtime proof still needs hosted, dedicated, JIP and HC smoke before changing role gates, PVEH ordering or locality-sensitive AI/vehicle behavior.

## Script execution, dynamic compile and scheduler waits - OA-safe but trust-sensitive

These commands are old enough for OA 1.64 and are core to this fork. The risk is **not availability**; it is authority, scheduler shape, file diagnostics and copying modern Arma 3 examples from mixed BI pages.

| Command family | OA status / BI note | Wasp source anchor | Developer guardrail |
| --- | --- | --- | --- |
| `call` / `spawn` | OA-safe program-flow commands. BI documents `call` as executing compiled code in the current context and `spawn` as creating a scheduled script handle; spawned code order is not guaranteed. | PVF dispatchers intentionally run handlers scheduled: `Server/Functions/Server_HandlePVF.sqf:14`, `Client/Functions/Client_HandlePVF.sqf:22`. Hosted/local PVF paths also use `_pvf Spawn ...` in `Common/Functions/Common_SendToClient.sqf:19`, `Common_SendToClients.sqf:17`, `Common_SendToServer.sqf:17`. Client handler fan-out spawns special/UI work in `Client/PVFunctions/HandleSpecial.sqf:9-33`. | Do not convert dispatcher `Spawn` to `Call` as a cosmetic performance fix. Some handlers contain sleeps or UI waits; switching scheduling needs a per-handler audit. Prefer direct `Call` only for known non-suspending, trusted helper code. |
| `compile`, `preprocessFile`, `preprocessFileLineNumbers` | OA-safe compile/preprocessor family. BI notes `preprocessFileLineNumbers` adds `#line` metadata for better runtime file/line errors, while `preprocessFile` performs uncached file reads and should not be used in hot loops. | Boot compiles most common/client/server functions: `Common/Init/Init_Common.sqf:6-160`, `Client/Init/Init_Client.sqf:89-140`, `Headless/Init/Init_HC.sqf:4-7`. PVF handler symbols are generated from controlled command lists in `Common/Init/Init_PublicVariables.sqf:44,49`. | Prefer `preprocessFileLineNumbers` for compiled mission files. Existing plain `preprocessFile` config/helper callsites are load/init-time patterns, not a license to compile inside gameplay hot loops. |
| `call compile` / `Call Compile Format` | OA-safe syntax, but injection/perf-sensitive. Use only when the string is source-controlled or built from allowlisted command names. | PVF dispatcher lookup still resolves string variable names at `Server_HandlePVF.sqf:14` and `Client_HandlePVF.sqf:22`; send helpers build per-channel variable names at `Common_SendToClient.sqf:17,20`, `Common_SendToClients.sqf:15,18`, `Common_SendToServer.sqf:15`, `Common_SendToServerOptimized.sqf:15`. Other controlled examples: CoIn varname setup `Client/Init/Init_Coin.sqf:86`, localized message formatting `Common/Functions/Common_SendMessage.sqf:26`. | Separate dispatcher hardening from per-handler authority validation. DR-1/DR-38 should replace per-message dynamic lookup with a precompiled allowlist, but handlers still need their own side/funds/range/object checks. Never `call compile` sender-controlled strings. |
| `execVM` / `execFSM` | OA-safe script/FSM launch commands returning handles. | Init/runtime scripts are launched from `initJIPCompatible.sqf:57,254,259`, `Client/Init/Init_Client.sqf:47,264,267,356-378,574-586,781`, and `Server/Init/Init_Server.sqf:298,510-528,595-626`. The latent AI supply-truck path calls missing `Server/FSM/supplytruck.fsm` from `Server/AI/AI_UpdateSupplyTruck.sqf:17`. | Keep script launch ownership clear: client UI/FX on clients, server loops on server, HC boot on HC. Before reviving `AI_UpdateSupplyTruck`, restore/design the FSM and guard the configs; do not assume `execFSM` failure is harmless. |
| `waitUntil`, `sleep`, `uiSleep` | OA-safe scheduler primitives. BI documents `waitUntil` as checking often until true and recommends adding `sleep` inside polling conditions where possible; `uiSleep` is A2-era and real-time based. | Good throttled wait: `Common/Common_AARadarMarkerUpdate.sqf:75`. Existing timeout-less waits: `Common/Common_MarkerUpdate.sqf:4`, `Client/Init/Init_Markers.sqf:11,27,34`, `Client/PVFunctions/LocalizeMessage.sqf:30`. Known hot/no-sleep classes are already tracked in `Hosted-Server-FPS-Loop-Sleep` and `WASP-Marker-Wait-Cleanup`. AntiStack uses `uiSleep` for real-time cadence in `Server/Module/AntiStack/mainLoop.sqf:16` and `flushLoop.sqf:15,30,55`. | Use `sleep`/`uiSleep` inside long polling loops. Add explicit timeout/logging for JIP/lifecycle waits where a missed broadcast can strand a client. Do not import A3 `waitUntil` array/handle overload examples into OA snippets. |
| `scriptDone` / `terminate` | OA-safe handle-management commands for handles returned by `spawn`/`execVM`; BI notes `terminate` aborts scheduled scripts on the next scheduler processing pass. | CoIn waits for a spawned border builder with `waitUntil {scriptDone _createBorderScope}` in `Client/Module/CoIn/coin_interface.sqf:342`. Tactical text animation cancels existing handles before replacing them in `Client/GUI/GUI_Menu_Tactical.sqf:301-322,367,382,466,510,521,676,764`. UAV module terminates a previous loop handle at `Client/Module/UAV/uav.sqf:77`. | Store handles if later cancellation/status matters. Guard `terminate` with `scriptDone`/nil checks like the tactical UI does. This does not replace state cleanup; terminated scripts may need explicit marker/UI/object cleanup paths. |

A3 contrast: `compileFinal` and `canSuspend` are Arma 3-only, `remoteExec`/`remoteExecCall` remain off-limits, and many modern BI examples use A3 `params` syntax. Convert examples to OA `_this select N` plus this mission's PVF/publicVariable wrappers before writing patch guidance.

Official references checked for this section: [call](https://community.bohemia.net/wiki/call), [spawn](https://community.bohemia.net/wiki/spawn), [execVM](https://community.bohemia.net/wiki/execVM), [execFSM](https://community.bohemia.net/wiki/execFSM), [compile](https://community.bohemia.net/wiki/compile), [preprocessFile](https://community.bohemia.net/wiki/preprocessFile), [preprocessFileLineNumbers](https://community.bohemia.net/wiki/preprocessFileLineNumbers), [waitUntil](https://community.bohemia.net/wiki/waitUntil), [sleep](https://community.bohemia.net/wiki/sleep), [uiSleep](https://community.bohemia.net/wiki/uiSleep), [scriptDone](https://community.bohemia.net/wiki/scriptDone) and [terminate](https://community.bohemia.net/wiki/terminate). A3-only contrast pages: [compileFinal](https://community.bohemia.net/wiki/compileFinal), [canSuspend](https://community.bohemia.net/wiki/canSuspend), [remoteExec](https://community.bohemia.net/wiki/remoteExec), [params](https://community.bohemia.net/wiki/params).

## Object scans & spatial queries — all OA-safe; pick the right one

These are heavily used across the fork and all date to OFP/ArmA/A2, so the trap here is **semantic, not version**: choosing a scan whose filter silently excludes what you need.

| Command | First · version | Returns / when to use | Repo |
| --- | --- | --- | --- |
| `nearestObjects [pos, classes, radius]` | **A2 · 1.00** | Array **sorted by distance**; `isKindOf` class matching; `[]` = all classes (slowest). | Supply command-center scan (`Server/Module/supplyMission/supplyMissionStarted.sqf:45,61`) — currently `[]` then filters for `Base_WarfareBUAVterminal` (DR-39). 16 files. |
| `nearEntities [classes, radius]` | **A2 · 1.00** | Unsorted; **alive units / vehicles / game-logics only** — excludes dead objects, buildings/structures, and crew inside vehicles. BI notes it is *"much faster"* than `nearestObjects` for soldiers/vehicles. | Camp/depot/airfield proximity (`nearEntities [WFBE_Logic_Camp, _range]`; `Client_GetClosestCamp/Depot/Airport.sqf`). 39 files. |
| `nearObjects [classes, radius]` | ArmA 1.00 | Unsorted radius scan, optional class filter; **does** include structures/dead objects. | 5 files. |
| `nearestObject` | OFP 1.00 | Single nearest object of a type (a hardcoded-50 m form exists). | 27 files. |

> **Guardrail (DR-39):** the supply command-center scan targets the `Base_WarfareBUAVterminal` **structure**, so it cannot be "optimized" by swapping to `nearEntities` — that command returns no buildings and would silently match nothing. The OA-safe narrowing is a **class-filtered** `nearestObjects` (or `nearObjects`) plus `isKindOf`, not an entity scan. See [Supply mission scan narrowing](Supply-Mission-Scan-Narrowing) and [Performance opportunity sweep](Performance-Opportunity-Sweep).

## AI waypoints and command movement - OA-safe with locality caveats

These commands are OA-era and useful in this fork, but the trap is copying newer Arma 3 examples or forgetting AI locality. Wasp already has one good defensive pattern in the bought-unit helper: validate the current waypoint index, then fall back to the engine's expected destination before ordering newly spawned units.

| Command family | OA status / BIKI note | Wasp source anchor | Developer guardrail |
| --- | --- | --- | --- |
| `currentWaypoint`, `waypoints`, `waypointPosition` | OA-safe. `currentWaypoint` returns the active waypoint index for the group; compare it with `count waypoints` before indexing. | `Client/Functions/Client_SendSpawnedUnitsToLeaderWaypoint.sqf:38-42`; diagnostic mirror in `Client_DiagnosePlayerAI.sqf:148-152`. | Keep the `_currentWaypoint < count (waypoints _team)` guard; do not assume `[group,index]` comes back from `currentWaypoint`. |
| `expectedDestination` | OA-safe engine planning probe returning a position plus mode; `DoNotPlan` means the destination is not useful for movement follow-up. | `Client_SendSpawnedUnitsToLeaderWaypoint.sqf:49-63`; `Client_RecoverPlayerAI.sqf:244`; `Client_WatchdogPlayerAI.sqf:214`. | Good fallback when a leader/group waypoint is absent, but always check the mode before using the position. |
| `commandMove` | OA-safe, but locality-sensitive: remote execution may show radio/order feedback without moving the remote AI. | `Client_SendSpawnedUnitsToLeaderWaypoint.sqf:80,85`; duplicate attack-wave-detail helper at `Client_SetAttackWaveDetails.sqf:80,85`. | Keep `commandMove` near the machine that owns/controls the player AI unless an OA MP smoke test proves another placement. For server-owned AI, prefer server waypoint builders. |
| `addWaypoint`, `setCurrentWaypoint`, `setWaypointType`, `setWaypointCompletionRadius`, `setWaypointStatements` | OA-safe waypoint builders. Modern BIKI examples include Arma 3-specific exact-placement radius behavior; do not import that into OA patch snippets. | `Common/Functions/Common_WaypointsAdd.sqf:27-34`; `Server/AI/Orders/AI_WPAdd.sqf:30-38`; UAV pathing in `Client/Module/UAV/uav.sqf:89-115`. | Compute the index from `count (waypoints _team)` before `addWaypoint`, then apply setters to `[_team, _WPCount]`. Starting the first waypoint with `setCurrentWaypoint` matches the existing Wasp pattern. |

Official references checked for this section: [currentWaypoint](https://community.bohemia.net/wiki/currentWaypoint), [waypointPosition](https://community.bohemia.net/wiki/waypointPosition), [expectedDestination](https://community.bohemia.net/wiki/expectedDestination), [commandMove](https://community.bohemia.net/wiki/commandMove), [addWaypoint](https://community.bohemia.net/wiki/addWaypoint) and [setCurrentWaypoint](https://community.bohemia.net/wiki/setCurrentWaypoint). Use the version badge and OA-era syntax as authority; later Arma 3 examples are contrast material only.

## Event handlers and local markers - OA-safe, locality-sensitive

These commands are available in Arma 2/OA and are already load-bearing in Wasp. The trap is not version support; it is locality, stacking and local/global state semantics.

| Command family | OA status / BI note | Wasp source anchor | Developer guardrail |
| --- | --- | --- | --- |
| `addEventHandler` | OA-safe object event handler. BI documents that multiple handlers can be added and do not overwrite older handlers. | Vehicle/unit creation: `Common/Functions/Common_CreateVehicle.sqf:36-37,52`, `Common/Functions/Common_CreateUnit.sqf:77`. HQ and repair paths: `Server/Construction/Construction_HQSite.sqf:36-38,89,93`, `Server/Functions/Server_MHQRepair.sqf:37-40`. Supply mission: `Server/Module/supplyMission/supplyMissionStarted.sqf:11`. | Treat repeated `addEventHandler` on reused objects as a possible leak. The supply truck path is a current example: repeated starts can stack `Killed` handlers unless a guard, stored EH id or removal shape is added. A client-side duplicate-handler example also exists around repeated `HandleReload` `Fired` handlers in `Client/Functions/Client_BuildUnit.sqf:332-334`. Keep gameplay authority in the handler's trusted/server path, not in a client-side affordance. |
| `addMPEventHandler` | OA 1.55 multiplayer event handler. BI notes MP handlers are added globally, with locality caveats by event; `MPRespawn` fires where the respawned unit is local. | `Server/AI/AI_AddMultiplayerRespawnEH.sqf:1` adds `MPRespawn` and immediately gates the body with `isServer` and `!(isPlayer ...)`. | Preserve the server/non-player guard. If extending beyond `MPRespawn`, read the specific BI event-handler locality row and smoke dedicated MP before assuming where the code fires. |
| `displayAddEventHandler`, `ctrlAddEventHandler`, `findDisplay`, `disableSerialization` | OA-safe UI/display/control commands. BI lists display/ctrl handlers as Arma 2-era commands; display/control values are client UI objects, so scripts commonly wait until the display exists and call `disableSerialization` when storing UI handles. | Main-display hotkeys: `Client/Init/Init_Client.sqf:237-262`. WASP map marker UI: `WASP/global_marking_monitor.sqf:60-81`. Bipod keybind: `Common/Functions/Common_Bipod.sqf:5`. CoIn display handlers: `Client/Module/CoIn/coin_interface.sqf:59-62`. UAV map control handlers: `Client/Module/UAV/uav_interface_oa.sqf:112-163`. | Use these only in client/UI paths. Wait for the intended display/control, throttle waits (`sleep` inside `waitUntil`) and avoid ambiguous `findDisplay` assumptions where IDDs collide. Current docs already track the WASP display-54 busy wait and dialog IDD collision lanes. Prefer the UAV/CoIn cleanup pattern when possible: store the returned handler id and remove that exact id (`Client/Module/UAV/uav_interface_oa.sqf:112-163`, `Client/Module/CoIn/coin_interface.sqf:59-62,329-332`). |
| `createMarkerLocal`, local marker setters and `deleteMarkerLocal` | OA-safe local marker family. BI documents local markers as machine-local and warns to use the local marker commands with them. | Team/town/base markers: `Client/FSM/updateteamsmarkers.sqf:21-25,160-181`, `Client/Init/Init_Markers.sqf:22-48`, `Client/Init/Init_BaseStructure.sqf:24-45`. Common local marker animation/update helpers: `Common/Common_MarkerUpdate.sqf:22,49-245`, `Common/Common_AARadarMarkerUpdate.sqf:10-198`. Respawn selector: `Client/Functions/Client_UI_Respawn_Selector.sqf:14-35`. | Local markers are correct for per-client HUD/map state. They are not global/JIP feature state. For revived MASH/paratrooper marker features, keep server-held marker state plus resend/pull separate from each client's local marker rendering. Pair local creation with `deleteMarkerLocal`; do not mutate local markers with global marker commands unless that global promotion is explicitly intended. Keep the separate global-for-JIP model distinct: `Common/Functions/Common_CreateMarker.sqf:51-83` creates globally-visible marker names, then applies local visual properties and broadcasts a marker-creation event for side-specific rendering. |

A3 contrast: do not import `addMissionEventHandler` examples into OA mission SQF without an OA availability proof. The current source has no live `addMissionEventHandler` use, while BI's command page routes its event list through Arma 3 mission event handlers and modern examples often use A3-only `params` syntax.

Official references checked for this section: [addEventHandler](https://community.bohemia.net/wiki/addEventHandler), [Arma 2 event handlers](https://community.bohemia.net/wiki/Arma_2:_Event_Handlers), [addMPEventHandler](https://community.bohemia.net/wiki/addMPEventHandler), [displayAddEventHandler](https://community.bohemia.net/wiki/displayAddEventHandler), [ctrlAddEventHandler](https://community.bohemia.net/wiki/ctrlAddEventHandler), [findDisplay](https://community.bohemia.net/wiki/findDisplay), [disableSerialization](https://community.bohemia.net/wiki/disableSerialization), [createMarkerLocal](https://community.bohemia.net/wiki/createMarkerLocal), [deleteMarkerLocal](https://community.bohemia.net/wiki/deleteMarkerLocal) and [addMissionEventHandler](https://community.bohemia.net/wiki/addMissionEventHandler) as an A3-era contrast warning.

## Object lifecycle, global init and local visual objects - OA-safe with locality/JIP caveats

These commands are the fork's object lifecycle vocabulary: create the real network object, run any JIP-visible init needed for old OA clients, clean it up deliberately, and keep client-only preview/FX objects local. The trap is copying modern Arma 3 helper commands from mixed BIKI pages, or replacing OA's old `setVehicleInit` pattern without preserving JIP-visible initialization.

| Command family | OA status / BI note | Wasp source anchor | Developer guardrail |
| --- | --- | --- | --- |
| `createVehicle` / `createUnit` | `createVehicle` is OFP-era and global-effect; BI documents it as creating an empty object and notes `Logic` objects use `createUnit`. `createUnit` is OA-safe, but BI's page warns about group locality and modern examples mention A3-only `remoteExec`/`setGroupOwner`. | Central vehicle factory: `Common/Functions/Common_CreateVehicle.sqf:18,43-44,63`. Central unit factory: `Common/Functions/Common_CreateUnit.sqf:30,61-62,77,85`. Construction and repair create real structures/logics then broadcast base init: `Server/Construction/Construction_HQSite.sqf:23,32-33,64,97`, `Server/Functions/Server_HandleBuildingRepair.sqf:18,44,59-60,102,107`. | Use global creation for real mission objects and unit creation where the group/locality model is understood. Do not paste A3 `createVehicleCrew`, `remoteExec` or `setGroupOwner` examples into OA; create crew and assign/move it explicitly with existing Wasp factories. Smoke hosted/dedicated/JIP after moving any creation site. |
| `createVehicleLocal` | OA-safe local-effect object creation; BI documents that created objects are not transferred over MP and have `netId` `0:0`. | Client-only construction previews and helpers: `Client/Module/CoIn/coin_interface.sqf:151,560,573,589,594,635,640`. Visual-only CM/IRS/nuke particles and lights use the same family: `Client/Module/CM/CM_Flares.sqf:16,25,30,35,45`, `Common/Module/IRS/IRS_CreateSmoke.sqf:11,16,22,24,30`. | Keep previews, borders, particles and lightpoints local. Switching these to global `createVehicle` creates network/JIP cleanup obligations and can leak objects; switching real gameplay objects to local creation makes other machines unable to see or interact with them. |
| `deleteVehicle` | OFP-era global-effect deletion. BI's page warns terrain objects and players cannot be deleted, deletion becomes fully `objNull` on the next frame, and crew-in-vehicle deletion is delicate. | Server cleanup paths: dropped items (`Server/FSM/cleaners/droppeditems_cleaner.sqf:19,26,33`), generic delayed trash (`Common/Functions/Common_TrashObject.sqf:29`), HQ/building cleanup (`Server/Functions/Server_BuildingKilled.sqf:96`, `Server/Functions/Server_OnHQKilled.sqf:43`). Player/team cleanup preserves player/playable units in `Server/Functions/Server_OnPlayerDisconnected.sqf:102,109`. Town vehicle deletion risk DR-45 is at `Server/FSM/server_town_ai.sqf:205,214`. Paratrooper support manually deletes units before vehicles at `Server/Support/Support_Paratroopers.sqf:137,142-143`. | Treat deletion as a gameplay authority decision, not just object cleanup. Check player/crew/cargo/playable cases before deleting vehicles or units. For crewed vehicles in OA, use explicit ejection/unassign/manual crew deletion patterns and smoke them; `deleteVehicleCrew` is A3-only even though its page describes the modern problem clearly. |
| `setVehicleInit` / `processInitCommands` / `clearVehicleInit` | OA-safe old MP/JIP init propagation; BI documents `setVehicleInit` as propagated over MP and run by `processInitCommands`, with JIP clients receiving the statement. The same page says the command was disabled in Arma 3 for security. | Wasp relies on this for hardcoded init strings: central factories (`Common_CreateVehicle.sqf:43-44`, `Common_CreateUnit.sqf:61-62`), base structures (`Construction_SmallSite.sqf:120-121`, `Construction_MediumSite.sqf:135-136`, `Server_HandleBuildingRepair.sqf:59-60`), UAV/support drops (`Client/Module/UAV/uav.sqf:30-31`, `Server/Support/Support_ParaAmmo.sqf:40-41`, `Support_ParaVehicles.sqf:41-42`, `Support_Paratroopers.sqf:52,71`) and artillery reinit (`Server/Functions/Server_ProcessUpgrade.sqf:72-75`). Texture propagation is centralized in `Common/Functions/Common_AddVehicleTexture.sqf:8-223,230`. | In OA, do not "modernize away" these calls just because A3 removed them. Keep init strings source-controlled and hardcoded; do not pass user/PVF payloads into init strings. Use `clearVehicleInit` after temporary reinit when the source already does. Review `WASP/common/procInitComm.sqf:6-8` carefully before reuse: its quoted `_initCMD` shape is not a general safe-injection helper. |
| `enableSimulation` / `hideObject` | OA-safe local-effect object commands. `enableSimulation` is A2-era and local-effect; `hideObject` is A2 1.06/OA-safe and local-effect. A3 `enableSimulationGlobal` / `hideObjectGlobal` are not OA commands. | `mission.sqm` uses `enableSimulation false` heavily for town/camp/logic editor objects, e.g. `mission.sqm:128` and the `WF_Logic` init at `mission.sqm:3265`. Current source has no live `hideObject` hits and no `hideObjectGlobal` / `enableSimulationGlobal` hits. | Treat these as per-machine effects. If a future feature needs all clients or JIP to see hidden/disabled state, drive it through OA-compatible init/PV/state replay rather than A3 Global variants. Re-enable simulation before putting players into affected vehicles. |
| `setVehicleVarName` | Armed Assault / OA-safe local-effect string-name binding. BI's page notes it changes string representation and that scripts need an extra variable assignment/publicVariable step if they want an editor-like global variable name. | CoIn assigns dynamic local logic names with `call compile format ["BIS_coin_%1 = _coin; _coin setvehiclevarname 'BIS_coin_%1';", BIS_coin_lastID]` at `Client/Init/Init_Coin.sqf:84-88`. | This is not network propagation by itself. Keep dynamic names source-controlled/derived from local counters, and do not confuse object string names with authoritative replicated object state. |

A3 contrast: `createVehicleCrew`, `deleteVehicleCrew`, `hideObjectGlobal`, `enableSimulationGlobal`, `remoteExec` examples on lifecycle pages and A3 vehicle-randomization examples are contrast material only. For Wasp OA, use explicit `createVehicle`/`createUnit`, old `setVehicleInit`/`processInitCommands` when JIP-visible init is required, local `createVehicleLocal` for FX/previews, and explicit cleanup/state replay.

Official references checked for this section: [createVehicle](https://community.bohemia.net/wiki/createVehicle), [createUnit](https://community.bohemia.net/wiki/createUnit), [createVehicleLocal](https://community.bohemia.net/wiki/createVehicleLocal), [deleteVehicle](https://community.bohemia.net/wiki/deleteVehicle), [setVehicleInit](https://community.bohemia.net/wiki/setVehicleInit), [processInitCommands](https://community.bohemia.net/wiki/processInitCommands), [clearVehicleInit](https://community.bohemia.net/wiki/clearVehicleInit), [enableSimulation](https://community.bohemia.net/wiki/enableSimulation), [hideObject](https://community.bohemia.net/wiki/hideObject) and [setVehicleVarName](https://community.bohemia.net/wiki/setVehicleVarName). A3-only contrast pages: [createVehicleCrew](https://community.bohemia.net/wiki/createVehicleCrew), [deleteVehicleCrew](https://community.bohemia.net/wiki/deleteVehicleCrew), [hideObjectGlobal](https://community.bohemia.net/wiki/hideObjectGlobal), [enableSimulationGlobal](https://community.bohemia.net/wiki/enableSimulationGlobal).
## OA-safe but removed in Arma 3 — the inverse trap

These commands **exist and work in OA 1.64** but were **disabled in Arma 3** for security. An agent porting Arma 3 reflexes can fall for the *opposite* of the avoid-list above — assuming they are gone or unsafe and rewriting them — when they are load-bearing in this fork. Keep them.

| Command | First · version | OA status / repo use |
| --- | --- | --- |
| `setVehicleInit` | OFP:Elite / A2 · 1.00 (OA 1.50) | OA-safe; **disabled in Arma 3** (security). Global Effect — the init string is broadcast and runs on every machine. Repo passes only **hardcoded literal** init strings: textures via `Common/Functions/Common_AddVehicleTexture.sqf`, fixed init-script calls (`Client/Module/UAV/uav.sqf:30`, artillery in `Client/Functions/Client_FNC_Special.sqf:202`). Because the strings are constants, this is not a network-derived injection surface beyond the documented PVF dispatcher class (DR-1). 17 files. |
| `processInitCommands` | OFP:Elite / A2 · 1.00 (OA 1.50) | OA-safe; **disabled in Arma 3**. Runs the queued `setVehicleInit` statements once; paired immediately after `setVehicleInit` in `Common_CreateUnit.sqf`/`Common_CreateVehicle.sqf`, construction sites and UAV spawn. 19 files. |

> The MP-safe wrapper `WASP_procInitComm` (`WASP/common/procInitComm.sqf`) is compiled **commented-out** (`initJIPCompatible.sqf:241-245`), so the mission relies on these raw calls directly — the standard A2 pattern. See [WASP overlay](WASP-Overlay).

## Gaps folded into canonical indexes
The [External Arma 2 OA reference guide](Arma-2-OA-External-Reference-Guide) now routes future agents to this page for A3-only command forms such as `params`, `remoteExec`, `parseSimpleArray`, `setGroupOwner` / `groupOwner`, multi-index `select`, filter `select`, `apply`, `isEqualTo` and inline `private _var = value`. The former `apply` snippet in [Deep-review findings](Deep-Review-Findings) has been rewritten as an OA-safe `forEach` loop.

The two **inverse-trap** classes are now represented in the [compatibility audit](Arma-2-OA-Compatibility-Audit#inverse-trap-commands) and `agent-compatibility-audit.json`: (a) OA-safe commands commonly **mis-assumed A3-only** (`diag_tickTime`, `uiSleep` — both verified A2-era above), and (b) OA-safe commands **removed in A3** (`setVehicleInit`, `processInitCommands`). Both classes risk a future agent "fixing" working OA code. Instructions-For-Codex item 48 is canonicalized.

## Continue Reading
Canonical usage map: [External Arma 2 OA reference guide](Arma-2-OA-External-Reference-Guide) · Compatibility audit: [Arma 2 OA compatibility audit](Arma-2-OA-Compatibility-Audit) · Networking: [Networking and public variables](Networking-And-Public-Variables) · Findings: [Deep-review findings](Deep-Review-Findings) · Code map: [SQF code atlas](SQF-Code-Atlas)

> **Method:** BIKI command pages carry a per-command version badge; that badge — not the usage examples, which often include later Arma 3 notes — is the authority for OA availability. Forum threads are corroboration only, never the version source of truth. Verified 2026-06-02, lane `external-a2-docs-editorial-compression`.
