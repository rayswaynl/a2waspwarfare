# Backlog — 2026-07-06

Compiled post-consolidation (v89-cmdcon44, 70 PRs merged, 550 branches pruned).
Sources: git/gh live checks, MASTER-CONSOLIDATION-PLAN-2026-07-06, known owner items.

---

## Tier 1 — Needs owner decision (blocked on you)

### [DECISION-A] PR #760 — V2 cutover (fable/v2-cutover)
- **State:** OPEN, MERGEABLE, CLEAN. Ready to merge to build84.
- **Gates remaining:** T3 soak A/B run (sensing branch + cc45 release first); owner flag-menu (DECAP_ENABLE<=0 = V1 rollback still works).
- **Owner tasks:** (1) Set `MATCH_REPORT_ENABLED=1` on livehost + set `WASPSTAT_DISCORD_CHANNEL` to a test channel. (2) Confirm T3 soaks pass before merging. The cutover is dual-audited and lint-clean; only the soaks stand between this and promotion.

### [DECISION-B] PR #740 — Migration map (fable/cutover-migration-map)
- **State:** OPEN, CONFLICTING (DIRTY). Has a merge conflict vs current build84.
- **Action needed:** Rebase or re-merge onto current build84-cmdcon36 tip before merging. Content is a doc-only PR (no mission SQF); conflict is likely Init_CommonConstants or a .md file. Owner to greenlight the rebased version.

### [DECISION-C] cc44v flag-1 sensing test rerun
- **State:** Staged, queued behind cc45 release. Revert target: `deploy45.ps1`.
- **Owner task:** After cc45 drops on livehost, schedule a sensing-test session (SENSING-TEST-PLAN-2026-07-06.md). The test rerun is gated on cc45 being live first — do not run against cc44v state.

### [DECISION-D] After-match report ACTIVATION
- **State:** NOT-DONE from original instructions. Pipeline is built (PR #64 merged in miksuus-warfare). Not activated in production.
- **Owner tasks:** Set `MATCH_REPORT_ENABLED=1` in production env + set `WASPSTAT_DISCORD_CHANNEL` to a real Discord channel ID. Two env-var changes on the Game PC, then a pm2 restart of the miksuu-bot process.

### [DECISION-E] Website guides content update
- **State:** NOT-DONE from original instructions. The web/guides pages on miksuu.com are stale vs current V48+ game.
- **Owner task:** Provide updated content or approve the auto-generated draft. No agent has built this yet; it needs a content pass before a build agent can update the Next.js guide pages.

### [DECISION-F] Aircraft spawn safety at owned airfields
- **State:** NOT-DONE from original instructions (MASTER-CONSOLIDATION-PLAN §1: "Full item unbuilt. Low priority per agent's triage").
- **Impact:** Players can spawn aircraft at captured enemy airfields without safety checks. Build is straightforward once prioritized.
- **Owner task:** Confirm priority and accept the default fix scope (spawn blocked at airfields outside your owned radius, flag-gated default 0).

### [DECISION-G] PR #515 / #614 / #637 — V2 fold PRs
- **State:** #515 UNKNOWN mergeable (scale AICOM map bands by world size), #614 UNKNOWN (teleport order flush), #637 MERGEABLE (bootstrap stipend config log).
- **Disposition per consolidation plan:** #515 + #614 fold into V2 reconciliation; #637 is a standalone MERGE-TO-BUILD84 candidate.
- **Owner task:** Confirm #637 can be merged standalone now. #515 and #614 wait for V2 cutover lane.

### [DECISION-H] PR #726 / #732 — Press stack (FOLD-AT-CUTOVER)
- **State:** Both MERGEABLE. #726 (driver-press hook) and #732 (exempt pressing teams from UNSTUCK ladder). Flagged fold-at-cutover per consolidation plan.
- **Owner task:** Confirm these merge as part of V2 cutover (PR #760) rather than standalone.

### [DECISION-I] Miksuu PR #57 — guild-architect (feat/guild-architect)
- **State:** OPEN, UNKNOWN mergeable. Requires 2 manual Discord steps by the owner.
- **Owner tasks:** (1) Run the guild-architect CLI against the test guild to restructure channels/roles. (2) Verify layout then run against production guild. No code changes needed — this is a config-deploy step.

### [DECISION-J] Miksuu PR #51 — optional mods layer (docs/optional-mods-layer)
- **State:** OPEN, UNKNOWN mergeable. Needs 4 `.bikey` files committed to the repo before this docs PR is accurate.
- **Owner task:** Locate and add the 4 `.bikey` files for the documented optional mods (STHUD, STGI, NameTags, no-blur, DSAI, GDT — see wasp-optional-mods.md memory). Or confirm the doc should ship without them and the .bikeys are distributed separately.

### [DECISION-K] Discord chat bridge PR #58 — dormant (feat/discord-chat-bridge)
- **State:** PR merged in miksuus-warfare. Relay BUILT but DORMANT. Likely fix identified: Miksuu's hosts-file blackholes `ibattle.org -> 127.0.0.1` (BattlEye's update phone-home = what bricked the prior attempt).
- **Owner task:** Test: comment out the `ibattle.org` entry in the Game PC hosts file and restart the BE-linked service, then retry the bridge. If it connects, the fix is permanent. Gate: owner consent to modify the hosts file on the Game PC.

---

## Tier 2 — Just needs doing (no owner decision required)

### [TODO-1] PR #754 — guer-cp-v2 (claude/guer-cp-v2)
- **State:** OPEN, UNKNOWN mergeable (GitHub compute pending). Branch settled — last commit 2026-07-06 12:12 CEST, no new activity.
- **Tip:** `ce0c8a9a5` (6 commits, feat + fix + TK/ZG mirrors, all flag-gated `WFBE_C_GUER_CP_V2=0`).
- **Merge-tree vs build84:** 3 conflicts, all in `Init_CommonConstants.sqf` across CH/TK/ZG — SML-1 tail block (build84 side) vs GUER-CP-V2 constant block (branch side). Both insertions are adjacent `isNil` guards, zero semantic overlap. Trivial sequential merge (append both blocks). Content-PASS from earlier verification still holds.
- **Action:** Merge agent can resolve the 3 Init_CommonConstants conflicts and push a conflict-free merge commit. No owner decision needed — all three inserting blocks are flag-gated default 0.

### [TODO-2] PR #717 — docs ledger (claude/fable-completion-push)
- **State:** OPEN, MERGEABLE. Docs-only: Fable completion push 2026-07-05 control docs + discovery artifacts.
- **Action:** Merge to build84 when ready. No conflicts.

### [TODO-3] PR #756 — wddm-functional-defenses (claude/wddm-functional-defenses)
- **State:** OPEN, CONFLICTING. Branch contains 9 commits (see summary below).
- **Action:** Needs rebase onto current build84 tip before merge review. Conflict surface is large (53 files, 2050 insertions) — rebase agent required.

### [TODO-4] PR #726 / #732 / #758 / #759 / #760 / #761 housekeeping
- **#758** (fix empty sound dialog): OPEN, MERGEABLE — clean standalone fix, merge anytime.
- **#759** (TK mirror catch-up): OPEN, MERGEABLE — mirror housekeeping, merge before any TK-touching PR.
- **#761** (2026-07-06 verified bug pack): OPEN, MERGEABLE — merge to build84.

### [TODO-5] 31 Cat-C branches — manual review
- Listed in MASTER-CONSOLIDATION-PLAN-2026-07-06 §4 "Category C". These have no open/closed PR and are not merged.
- Top candidates to close/delete: `event/napf-tank-war` (local worktree C:/Users/Steff/a2waspwarfare-napf — pre-existing Napf conflict markers in Modded_Missions tree, 5 files), `feature/zargabad-map` (oldest, 2026-06-04), `feat/client-fps` (local worktree a2wasp-aicom-review).
- **Action:** Owner or clean-up agent reviews each branch, opens a close-PR or deletes directly.

### [TODO-6] Napf event-mission conflict markers (pre-existing)
- **Location:** `Modded_Missions/[55-2hc]warfarev2_073v48co.Napf/` — 5 files with `<<<<<<<` markers: `Client/Module/Skill/Skill_Init.sqf`, `Common/Config/Core_Structures/Structures_CO_US.sqf`, `Common/Config/Core_Units/Units_RU.sqf`, `Common/Module/IRS/IRS_OnIncomingMissile.sqf`, `description.ext`.
- **State:** Pre-existing, not in any open PR. These are committed conflict markers, not workspace artifacts.
- **Action:** Dedicated fix PR to resolve the 5 files (pick one side or reconcile). Low urgency — Napf is a modded terrain, not a primary served map.

### [TODO-7] SML 2–5 build queue
- **State:** SML-1 (camp-split) is merged to build84 via `fable/sml1-camp-split`. SML 2–5 not built.
- **Next:** SML-2 is "dismounts" (per WASP squad micro layer memory). No PR open for it.
- **Action:** Dispatch a build agent for SML-2 when SML-1 has soak time on the live server.

### [TODO-8] Miksuu specfix worktree — half-done work
- **Location:** `C:/Users/Steff/miksuu-specfix` (branch `fix/spec-integration`).
- **State:** 2 unstaged files with real changes, 2 commits ahead of origin/main:
  - `web/src/components/motion/AmbientOpsLayer.module.css` — replaces hard-coded `45s` animation durations with CSS variable `--mw-motion-ambient` (correct spec fix).
  - `web/src/components/wasp/WarLogFeed.tsx` — replaces emoji icon glyphs with a new `WarLogEvent` import + `LogIcon` union mapping (major refactor toward spec compliance).
- **Problem:** `WarLogEvent` component is imported but likely does not exist yet (`@/components/motion/WarLogEvent`). The agent was killed mid-task. The worktree is also 2 commits behind origin/main (fast-forwardable).
- **Action:** A build agent needs to: (1) create `WarLogEvent.tsx` implementing the `LogIcon`/`LogSide` props, (2) ensure the `toLogIcon` mapping in `WarLogFeed.tsx` is complete, (3) git pull origin main to fast-forward, (4) stage and commit. This is a 30-60 min build task.

---

## Tier 3 — Waiting on external / time-gated

### [WAIT-1] PR #760 V2 cutover — T3 soaks
- Gated on cc45 release landing first, then a full T3 soak A/B session on the live server.

### [WAIT-2] cc44v sensing rerun
- Gated on cc45 release. Target deploy script: `deploy45.ps1` on livehost.

### [WAIT-3] Miksuu #58 Discord chat bridge
- Gated on owner testing the `ibattle.org` hosts-file hypothesis (see DECISION-K).

---

## Branch status summary

| Branch | PR | State | Mergeable | Last commit | Notes |
|---|---|---|---|---|---|
| claude/guer-cp-v2 | #754 | OPEN | UNKNOWN (3 conflicts vs build84) | 07-06 12:12 | Settled; trivial conflict resolve |
| claude/wddm-functional-defenses | #756 | OPEN | CONFLICTING | 07-06 12:51 | Needs rebase; see summary below |
| fable/v2-cutover | #760 | OPEN | MERGEABLE/CLEAN | — | Awaiting soaks + owner flag menu |
| fable/cutover-migration-map | #740 | OPEN | CONFLICTING | — | Needs rebase; doc-only PR |
| claude/fable-completion-push | #717 | OPEN | MERGEABLE | — | Docs, clean |
| fable/aicom-v2-l1-press | #726 | OPEN | MERGEABLE | — | Fold at V2 cutover |
| fable/aicom-v2-l1-press-fix | #732 | OPEN | MERGEABLE | — | Fold at V2 cutover |
| fable/fix-empty-sound-dialog | #758 | OPEN | MERGEABLE | — | Standalone bug fix |
| fable/mirror-tk-catchup | #759 | OPEN | MERGEABLE | — | Mirror housekeeping |
| claude/hunt-2026-07-06 | #761 | OPEN | MERGEABLE | 07-06 | Bug pack |

---

## WDDM-functional-defenses summary (PR #756)

9 commits, 53 files, +2050 / -69 lines vs build84. All changes behind flags defaulting 0.

**What it does:**
1. **Factory wall slabs v4** (`WFBE_C_WALLS_V4`, default 0) — redesigns the v3 slab layer: contiguous runs at HQ-pitch, +X face stays fully open (vehicle delivery path), walking gaps preserved, `Land_CncBlock_Stripes` accents at gap mouths. Barracks/Light/Heavy/Aircraft/CommandCenter variants. Auto-generated from `docs/design/compositions/*_walls_v4.wddm.json`.
2. **Manned fort composition variants** (`WFBE_C_DEF_FORT_MANNED`, default 0) — WEST + EAST variants of Strongpoint, Checkpoint, and Observation Post with crewed positions.
3. **Stale v2-ladder composition drafts retired** — 6 `.wddm.json` files renamed with `_v2_reverted_` prefix; compositions README rewritten.
4. **DEFMENU v2 seat-count label fixes** — corrects factually wrong AI seat counts in the defense menu (Arty Pit L=2 AI not 3; Mixed Strtp H=3 AI not 4). No flag; these are label-only corrections.
5. **Fortification pack** (`WFBE_C_DEF_FORTIF_PACK`, default 0) — 5 passive WDDM compositions (wall row/corner, tall LoS screen, HESCO line, gate complex) plus menu wiring in `Core_CIV.sqf`. Gets its own composition cap (`WFBE_C_DEF_FORTIF_CAP` default 6) split from the standard WDDM cap, with pool-split logic in `RequestDefense.sqf` so fortif and weapon placements don't share the cap count. LoS Screen fix: `Base_WarfareBBarrier10xTall` children (not in side DEFENSENAMES) are counted via `WFBE_FORTIF_COUNT_EXTRA` auxiliary list.

**GUER/owner-rule check:** No violations found. The Init_Defenses.sqf comment explicitly preserves GUER/TKA volume ("GUER/TKA note: every other side resolves _EAST → the DShKM variants"). Owner intent quote included verbatim in comments. No caps or nerfs to GUER output. `RequestDefense.sqf` pool-split is symmetric across sides. No owner-restricted patterns (`TPWCAS`, `WFBE_C_SIM_GATING`, antistack) touched.

**Conflict:** The branch is CONFLICTING vs current build84 (it was branched from an earlier build84 state). Needs a rebase pass before merge review.

---

## Ranked impact order

1. PR #760 V2 cutover — highest impact, all gates documented, T3 soaks + owner flag menu are the only blockers
2. PR #761 bug pack — merge immediately (MERGEABLE, no gate)
3. PR #758 empty sound fix — merge immediately (MERGEABLE, no gate)
4. PR #759 TK mirror — merge before any TK-touching work (MERGEABLE)
5. TODO-1 guer-cp-v2 (#754) — trivial conflict resolve, content-PASS already verified
6. TODO-8 specfix worktree — half-done, `WarLogEvent.tsx` missing; needs a focused build session
7. DECISION-D match-report activation — 2 env vars on Game PC, owner step, immediate community value
8. TODO-6 Napf conflict markers — low urgency, modded terrain
9. TODO-7 SML 2-5 — queue after SML-1 soak time
10. TODO-5 31 Cat-C branches — cleanup, no gameplay impact
