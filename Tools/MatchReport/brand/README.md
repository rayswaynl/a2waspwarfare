# Brand assets — Miksuu's Warfare

Mirrors the brand kit in `miksuus-warfare/brand/` so the report matches the site.

- `fonts/` — Oswald (display), Inter (sans), JetBrains Mono (numbers). All OFL-licensed.
- `logo/mark.png` — the mark (512², transparent), used on intro + winner + footer.
- `logo/lockup.png`, `logo/mark.svg` — reference copies.

Palette tokens live in `../render.py` (gunmetal / steel / bone / orange + faction
blue/red/olive), mirroring `miksuus-warfare/brand/tokens.css`. Update both together.
